---
title: Enforcing Dirichlet Boundary Conditions in Operator Learning
title_zh: 在算子学习中强制执行狄利克雷边界条件
authors: "Andrew M. Stuart, Margaret Trautner"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27256v1"
tags: ["query:chemical-llm"]
score: 8.0
evidence: 在算子学习中显式施加边界条件，构建面向过程的数学模型
tldr: 科学机器学习中的算子学习常需逼近偏微分方程解算子，边界条件往往已知却仅被隐式引入。现有显式强制边界的方法受限于边界光滑、均匀网格和盒状域。本文提出新架构，令每层输出位于Laplacian齐次Dirichlet特征函数张成空间，独立于训练严格满足边界条件，且保留核积分算子表达力。该方法只需输出域有界、Lipschitz边界，不限制离散化与几何。作者证明该架构以及一类核积分算子的通用逼近性，并在Darcy流与Helmholtz方程上验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.27256v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 831, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1009, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1008, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1018, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1020, \"height\": 671, \"label\": \"Figure\"}]"
motivation: 现有算子学习要么从数据隐式学边界条件，要么显式强制但受限于光滑边界、均匀网格和盒状域，缺乏通用且保持表达力的方案。
method: 将每层输出约束在输出域上Laplacian的齐次Dirichlet特征函数张成的子空间，从而严格满足边界条件；仅要求域有界且边界Lipschitz，允许任意离散化和几何。
result: 证明该架构具有通用逼近性质，并统一了多类核积分算子的理论；在2D Darcy流和Helmholtz方程上与替代方法比较，验证了有效性。
conclusion: 该方法为算子学习提供了独立于训练、适配任意网格和几何的严格Dirichlet边界条件强制手段，同时保持表达力并具备理论保证。
---

## 摘要
科学机器学习中的算子学习关注的是无限维函数空间之间映射的逼近；这类映射常作为偏微分方程（PDE）的解算子出现。神经算子在从数据逼近此类映射方面已展现出广泛的实证成功。然而，大多数现有的神经算子架构通过从数据训练来间接强制边界条件，尽管边界条件通常是精确已知的。此外，现有的显式强制边界条件的修改和方法受到不切实际的限制，包括边界光滑性、均匀网格以及可分离的盒状域。在这项工作中，我们提出了一种架构，该架构独立于训练，满足齐次狄利克雷边界条件，同时保留现有核积分神经算子架构的表达能力。这是通过强制每层的输出包含在输出域上拉普拉斯算子的齐次狄利克雷特征函数的子空间中来实现的。该方法仅要求输出域具有利普希茨边界的有界域，并且对离散化选择没有任何限制，使其适用于任意网格数据和一般几何形状。我们证明了所得架构的普适逼近性；此外，我们在分析中采用的方法证明了一类广泛的核积分神经算子的普适性，从而统一了各种算子学习方法的现有理论。我们在二维PDE中由系数到解映射定义的映射上验证了所提出的方法：方形域上的达西流和圆形域上的亥姆霍兹方程。并与替代方法进行了比较。

## Abstract
Operator learning in scientific machine learning is concerned with approximation of maps between infinite-dimensional function spaces; such maps frequently arise as the solution operators of partial differential equations (PDEs). Neural operators have demonstrated broad empirical success at approximating such maps from data. However, most existing neural operator architectures enforce boundary conditions indirectly through training from data even though the boundary condition is often known exactly. Furthermore, existing modifications and approaches that do enforce boundary conditions explicitly suffer from impractical restrictions, including boundary smoothness, uniform grids, and separable, box-like domains. In this work, we propose an architecture which, independently of training, satisfies homogeneous Dirichlet boundary conditions, whilst simultaneously retaining the expressivity of existing kernel-integral neural operator architectures. This is achieved by enforcing the property that the output of each layer is contained in the span of a subset of the homogeneous Dirichlet eigenfunctions of the Laplacian on the output domain. The method requires only that the output domain be bounded with Lipschitz boundary and places no restriction on the choice of discretization, making it applicable to arbitrary mesh data and general geometries. We prove universal approximation for the resulting architecture; furthermore the approach we adopt in the analysis proves universality for a broad class of kernel-integral neural operators thereby uniting existing theory for a variety of operator learning methods. We validate the proposed method on maps defined by the coefficient to solution map in 2D PDEs: Darcy flow on a square domain and the Helmholtz equation on a circular domain. Comparisons are made with alternative methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

**研究动机与背景：**

- 算子学习（Operator Learning）旨在用神经网络逼近无限维函数空间之间的映射，这类映射通常对应偏微分方程（PDE）的解算子。
- 大多数现有神经算子架构（如 DeepONet、FNO、PCA-Net 等）只通过训练数据隐式地让模型学习边界条件，而边界条件本身往往是已知的且可精确表达。
- 已有显式强制边界条件的方法存在实用限制，包括：
  - 要求边界光滑；
  - 依赖均匀网格（如 Chebyshev-Gauss-Lobatto 点、均匀网格）；
  - 仅适用于可分离的盒状域（box-like domain）；
  - 对任意网格和复杂几何的适用性不足。

**核心问题：**

- 能否设计一种神经算子架构，使其输出**与训练无关地**严格满足齐次 Dirichlet 边界条件，同时保留核积分神经算子的表达能力，并适用于任意网格与一般几何域？

## 2. 论文提出的方法论

**核心思想：**

- 利用输出域上 Laplacian 算子的**齐次 Dirichlet 特征函数**作为神经网络隐藏层的基函数。
- 由于这些特征函数在边界上取值为零，只要输出层叠加以零为偏置的零保持激活函数和投影层，网络输出就自动满足齐次 Dirichlet 边界条件，**无需任何训练**。

**关键技术细节：**

1. **Nonlocal Neural Operator（NNO）/ 核积分神经算子**：定义了一个通用架构，由提升层、隐藏层（核积分形式）和投影层组成；FNO 是该框架的特例。
2. **Dirichlet Layer（狄利克雷层）**：将 NNO 隐藏层中的基函数ψ和φ设为 Laplacian 的 Dirichlet 特征函数{η_m}，并将该层作为最终隐藏层，配合零偏置的投影层，即可严格满足零边界条件。
3. **处理非零边界**：通过预处理/后处理实现——学习映射到零边界输出（u - b·1），再在输出上加回边界值b·1。
4. **理论保证**：
   - **Theorem 3.4**：单个隐藏层、单对基函数即可实现通用逼近（Cs′意义下）。
   - **Theorem 3.5**：利用架构自身基函数实现通用逼近，适用范围更广。
   - **Corollary 3.6**：Dirichlet 层在 C(Ω) 意义下具有通用逼近性，且输出在边界上严格为零（迹意义）。
5. **Smoothed Approximation Assumption (Assumption 3.1)**：统一的抽象假设，涵盖 Fourier 基、Dirichlet 特征函数基、Legendre 多项式基等，使得证明框架适用于多种核积分神经算子。

**计算复杂度：**

- 每个 Dirichlet 层的复杂度为 O(NM)，N 为网格点数，M 为特征函数数；实际中 M ≈ log N 足够，因此与 FNO 的 O(N log N) 复杂度相当。

## 3. 实验设计

**实验场景一：Darcy 流（方形域）**

- 方程：−∇·(a∇u) = 1, u|∂Ω = 0，Ω = [0,1]²。
- 数据集：来自 FNO 原论文，共 2048 个样本；248 用于验证（超参数调优），200 用于测试。
- 网格：421×421 均匀网格（有限差分求解）。
- 对比方法：
  - 标准 FNO；
  - FNO 输出在训练后投影到 Laplacian 特征函数基（"projected after training"）；
  - FNO 输出在训练过程中投影（"projected during training"）；
  - 正则化最小二乘（岭回归）在特征函数系数空间中的线性映射；
  - 本方法提出的 Dirichlet Neural Operator（DNO）。

**实验场景二：Helmholtz 方程（圆形域）**

- 方程：−Δu − ω²au = 0, u|∂Ω = b，ω = 5π/2，Ω 为单位圆盘。
- 输入：系数场a（高斯叠加归一化）+ 标量边界值 b；
- 输出：解 u；学习时先减去 b·1 使输出满足零边界，再在预测后加回。
- 数据生成：FEniCSx 有限元方法（CG1 元）在三角网格上求解；11000 个样本，500 验证、500 测试；剔除了接近奇异点的样本（max|u|/b > 10）。
- FNO 适配：将三角网格插值到均匀网格，并外加零掩膜（超出圆盘区域的输入置零）——这给了 FNO 一定优势（误差不再经历返回插值）。

**训练超参数（经网格搜索）：**

- Darcy：DNO 12 modes/维、宽度64、300 epochs、batch size 15、4 个 FNO 层 + 1 个 Dirichlet 层；FNO 对应类似配置。
- Helmholtz：DNO 16 modes、宽度64、300 epochs、batch size 25、4 个 Dirichlet 层；FNO 12 modes、宽度64、25 batch、300 epochs、4 层。

## 4. 资源与算力

**论文未明确说明 GPU 型号与数量。** 仅能从图 3、图 4（超参数搜索）的横轴估算训练时间：

- Darcy 示例训练时间约在几分钟到 200 分钟不等；
- Helmholtz 示例训练时间约在几分钟到 40 分钟左右；
- FNO 与 DNO 的训练时间大体相当。

除此之外，论文没有提供具体的硬件信息（如 GPU 型号、并行卡数等）。

## 5. 实验数量与充分性

**实验数量：**

- 两个 PDE 场景（Darcy 流、Helmholtz），覆盖了方形均匀网格和圆形三角网格两种完全不同几何/离散方式；
- 每个场景进行超参数网格搜索（图 3、图 4）；
- 每个场景进行**数据效率比较**，训练数据规模从 N=10 到 N=1600（Darcy）及 N=10 到 N=10000（Helmholtz）；
- 对比了 5 种方法（DNO、FNO、训练后投影 FNO、训练中投影 FNO、岭回归），每次用 5 个模型取均值和 2 倍标准差（图 5、图 6）。

**充分性与客观性评估：**

- 优点：实验设计较全面，覆盖了低数据和高数据两种 regime；对比了多种替代显式强制边界的方法；基于 FNO 原论文的标准数据集（Darcy），保证可复现性。
- 不足之处：
  - 只有两个实验场景，缺乏更高维或更复杂域（如三维、带角点奇性域）的验证；
  - 未做严格的消融研究（如针对特征函数数量 M、网络深度等的系统消融表）；
  - 对 FNO 的 Helmholtz 实验需要插值到均匀网格，虽然文中称这种处理给 FNO 以优势，但本质上 FNO 在非均匀网格上的不公平劣势仍不可避免；
  - 训练时间对比只在图中给出，没有独立表格。

## 6. 论文的主要结论与发现

1. **DNO 在低数据 regime 下优势显著**：Darcy 示例中，N=100 以下 DNO 相对其他方法有约 2 倍精度提升；其他非线性方法甚至不如岭回归。
2. **DNO 在高数据 regime 下仍有优势**：Darcy（N=1600）时 DNO 比 FNO 提升约 20% 精度；Helmholtz（N=5000/10000）时 DNO 约为 FNO 精度的 2 倍。
3. **投影 FNO 并不总是有益的**：在 Darcy 中，投影（无论训练前/后）均未超过标准 FNO；在 Helmholtz 中，训练后投影使误差翻倍——说明 DNO 的优势不仅来自边界条件的满足，还来自架构本身与问题结构的匹配。
4. **DNO 对超参数选择更鲁棒**：从图 3、4 可见，DNO 在次优超参数下分散度更小、性能更稳定。
5. **理论上**：统一证明了一大类核积分算子（Fourier、Laplacian 特征函数、Legendre）的通用逼近性，为后续研究提供了统一分析框架。

## 7. 优点

- **独立于训练精确满足边界条件**：无需任何训练即可严格保证齐次 Dirichlet 边界条件（在迹意义下），这是与数据驱动学习本质不同的优势。
- **通用几何与任意离散化**：仅要求输出域有界且具 Lipschitz 边界，不限制网格类型，适用于任意有限元网格或点云数据。
- **理论完备**：不仅证明了所提架构的通用逼近性，还建立了一个统一的抽象框架（Smoothed Approximation Assumption），涵盖多种经典基函数选择。
- **实用计算开销**：复杂度与 FNO 相当，不引入显著额外成本。
- **可与现有架构兼容**：可仅将 FNO 的最后一层替换为 Dirichlet 层，即在 Darcy 实验中展示的混合架构。
- **可处理非零 Dirichlet 边界**：通过预处理/后处理简单扩展。

## 8. 不足与局限

- **逼近空间限制**：Corollary 3.6 的通用逼近仅在 C(Ω) 意义下成立；在一般 Lipschitz 域上，特征函数的 Cs′ 范数在边界附近可能发散（如重入角），因此无法得到更高阶范数（Cs′, s′>0）下的逼近保证。
- **特征函数计算成本**：对于复杂几何域，需要数值求解 Laplacian 特征问题（如有限元方法），这本身可能是一个不小的计算开销；论文未讨论这一预处理成本。
- **实验覆盖有限**：仅两个 2D 问题；未涉及三维问题、混合边界条件、Neumann/Robin 条件以及更复杂的非凸域。
- **混合边界条件未解决**：论文在结论中明确提出，如何在不同边界分区上自动强制混合边界条件仍是开放问题。
- **对非零边界条件处理略显间接**：需要额外预处理/后处理步骤（加/减 b·1），对于边界值为函数而非常数的情形，处理方式未展开明确分析。
- **无大规模算力基准**：没有报告 GPU 型号、训练吞吐量等，无法精确评估资源消耗的公平对比。

---

（完）
