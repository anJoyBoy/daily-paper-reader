---
title: "Mind the Gap: Bridging Behavioral Silos with LLMs in Multi-Vertical Recommendations"
title_zh: 注意差距：利用大语言模型在多垂直领域推荐中弥合行为孤岛
authors: "Nimesh Sinha, Raghav Saboo, Martin Wang, Sudeep Das"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06779v1"
tags: ["query:llm-dm"]
score: 8.0
evidence: 基于LLM的特征生成用于推荐系统，属于数据挖掘应用
tldr: 多垂直领域电商（如DoorDash）中新品类存在冷启动问题。本文利用大语言模型从数据丰富的餐厅垂直领域生成稀疏高维特征，通过分层检索增强生成管道从用户历史中提取多级分类特征，并集成到多任务学习排序模型中。离线与在线实验表明该方法显著提升新品类个性化推荐效果，有效弥合行为数据鸿沟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06779v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1432, \"height\": 317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 540, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 868, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 863, \"height\": 467, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06779v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1846, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06779v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1848, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06779v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1280, \"height\": 177, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06779v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1278, \"height\": 178, \"label\": \"Table\"}]"
motivation: 多垂直领域电商中新品类用户行为数据稀疏，冷启动问题突出，需跨垂直迁移知识以提升推荐质量。
method: 利用LLM进行生成式推理，通过分层RAG从餐厅订单历史合成多级分类特征，编码长期偏好与短期意图，集成到MTL排序模型。
result: 离线与在线评估表明该方法显著提升新品类个性化推荐与用户参与度。
conclusion: 基于LLM的特征生成有效桥接行为数据鸿沟，为数据稀疏的电商垂直领域提供可实践的个性化方案。
---

## 摘要
在像DoorDash这样的多垂直电商平台中，相对较新的产品垂直领域（如杂货和零售）为个性化创新提供了重大机会。一个关键挑战在于解决用户的“冷启动”问题。本文介绍了一种新颖的框架，通过将知识从数据丰富的垂直领域（例如DoorDash的餐厅）转移到数据稀疏的垂直领域来提升推荐质量。我们利用大型语言模型（LLMs）进行生成式推理，合成封装潜在用户偏好的稀疏高维特征。具体来说，我们采用分层检索增强生成（RAG）流水线，从用户餐厅订单历史和搜索查询中推导出多级分类特征。这些生成的特征编码了长期跨垂直偏好和短期意图，并被集成到生产中的多任务学习（MTL）排序模型中。通过大量离线和在线评估，我们证明该方法显著提升了新兴业务垂直领域的个性化和参与度，有效弥合了行为数据差距。

## Abstract
In multi-vertical e-commerce platforms like DoorDash, relatively newer product verticals such as grocery and retail present a significant opportunity for personalization innovation. A key challenge lies in solving the "cold start" problem for users. This paper introduces a novel framework for enhancing recommendation quality by transferring knowledge from data-rich verticals (e.g., restaurants at DoorDash) to data-sparse ones. We leverage Large Language Models (LLMs) to perform generative inference, synthesizing sparse, high-dimensional features that encapsulate latent user affinities. Specifically, we employ a hierarchical Retrieval-Augmented Generation (RAG) pipeline to derive multi-level taxonomic features from user restaurant order histories and search queries. These generated features, encoding both long-term cross-vertical preferences and short-term intent, are integrated into a production Multi-Task Learning (MTL) ranking model. We demonstrate through extensive offline and online evaluation that this approach significantly improves personalization and engagement in emerging business verticals, effectively bridging the behavioral data gap.

---

## 论文详细总结（自动生成）

# 论文总结：Mind the Gap: Bridging Behavioral Silos with LLMs in Multi-Vertical Recommendations

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多垂直领域电商平台（如DoorDash）在拓展新品类（杂货、零售、美妆等）时，面临用户行为数据稀疏导致的**冷启动问题**——新品类用户没有直接的历史交互记录。而平台在已有成熟垂直领域（如餐厅外卖）积累了丰富的用户行为数据。
- **核心问题**：如何利用来自数据富集垂直领域的知识，来提升数据稀疏垂直领域的个性化推荐质量。
- **整体含义**：这项工作提出利用大语言模型（LLM）作为“语义桥梁”，将用户在一个垂直领域（餐厅）的行为模式转化为可迁移的偏好特征，从而弥合行为数据孤岛，为冷启动用户提供更相关的推荐。这是LLM在工业级推荐系统中解决跨域冷启动问题的首次系统性实践尝试。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：使用大型语言模型（LLM）进行**生成式特征工程**——从用户非结构化的历史行为（餐厅订单名称、搜索查询）中，自动推断出结构化的多级分类偏好特征，并将这些特征注入到生产级的多任务学习（MTL）排序模型中。
- **关键技术细节**：
  - **分层检索增强生成（Hierarchical RAG）流水线**：先推断顶层（L1/L2）分类亲和性，再以这些结果为约束生成更细粒度（L3）的分类。这在计算上缩小搜索空间、降低幻觉风险，并提高特征精度。
  - **特征类别**：① **长期跨垂直偏好画像**——从餐厅订单历史推断；② **短期意图画像**——从近期搜索查询推断。
  - **提示工程与推理控制**：设置温度=0.1以确保确定性；要求模型输出置信度分数，仅保留≥0.8的类别；使用严格JSON格式输出；通过用户历史的时间顺序输入来捕捉偏好演变。
  - **模型选择与成本优化**：选择GPT-4o-mini替代GPT-4o以降低成本；使用提示缓存策略（静态指令和分类表缓存，仅动态拼接用户历史）；采用“即时特征物化”策略（仅当用户有新行为时才更新特征），总体计算成本降低约**80%**。
  - **特征物化与服务化**：每日离线批处理流水线生成特征，写入数据湖供训练，同时推送至在线特征存储供实时推理。
  - **MTL排序模型特征融合**：将LLM生成的特征 `u_LLM` 与原有用户特征 `u_eng` 拼接得到 `u_aug = [u_eng; u_LLM]`，再与物品特征 `i_eng` 拼接后送入共享MLP主干 `φ`，然后接任务特定预测头。可变长分类特征通过共享embedding表+mean pooling聚合。
- **公式**（文字说明）：
  - 总损失：`L = sum(t=1..T) α_t * L_t(ŷ_t, y_t)`，其中T为任务数量（如点击、加购、下单等），α_t为任务权重。
  - 最终预测：`z = φ([u_aug, i])`，`ŷ_t = σ(w_t^T z + b_t)`，σ为激活函数（如sigmoid）。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：在DoorDash真实生产环境中进行。使用**过去3个月的用户行为数据**（20%的用户采样）来训练LLM特征生成；离线评估使用**15天保留期**数据，以转化为正负样本标签。场景涵盖**通用用户群体**、**冷启动用户（新品类无历史交互）** 和**高活跃用户（新品类的power用户）**。
- **基准（Baseline）**：生产级MTL物品排序模型，仅使用原始用户历史交互特征和物品属性特征（不包含LLM生成的特征）。
- **对比方法**：本文的**Proposed Model**——在相同MTL架构上额外拼接LLM生成的特征（分为纯餐厅订单信号、纯搜索查询信号、二者联合信号做消融）。
- **评估指标**：AUC-ROC（分类性能）、MRR（排序质量）。同时做了特征质量的人评（Human Evaluation）和LLM-as-judge评价（以GPT-4o打分），以3分制（非个性化、部分个性化、高度个性化）衡量。

## 4. 资源与算力

- **未明确说明具体GPU型号、数量、训练时长**。论文主要聚焦于LLM的推理成本优化，提及使用了GPT-4o和GPT-4o-mini，并通过提示缓存、即时特征物化等方法将推理成本降低了约80%。但未披露MTL模型训练或LLM推理所消耗的具体算力资源（如GPU数量、训练小时数）。这一点需要在未来工作中补充。

## 5. 实验数量与充分性

- **离线实验**：对比了总体人群、冷启动人群、高活跃人群的AUC-ROC和MRR提升。消融实验分析了不同信号源（餐厅订单 vs 搜索查询 vs 二者联合）对各类消费者的贡献差异。
- **在线实验**：进行了**影子部署（Shadow Deployment）**——将LLM增强的模型与基线模型同时运行在真实流量上，对比其在线指标（AUC-ROC、MRR）。结果与离线一致，验证了实际效果。
- **特征质量评估**：人工评估（N=1000样本/信号源）和LLM作为评委（同样N=1000）的评分，展示了LLM生成特征的个性化程度分布。
- **充分性评价**：实验较为充分。覆盖了离线与在线、多人群、多信号源的消融，且评估指标覆盖分类和排序两个维度。不过缺少更细粒度的A/B实验（论文仅用影子部署替代了线上A/B），也未对比其他替代知识迁移方法（如传统矩阵分解跨域迁移或embedding映射等）。**客观性和公平性**方面，由于是在真实生产环境中与当前生产基线对比，具有较高外部效度，但内部消融仅对比自身变体，未与外部基线方法（如只用用户embedding、不使用LLM的跨域迁移）对比，有一定局限性。

## 6. 论文的主要结论与发现

- **总体性能提升**：LLM增强的排序模型在通用用户群体上获得**AUC-ROC相对提升4.4%**，**MRR提升4.8%**（离线）；在线影子部署中AUC-ROC提升4.3%，MRR提升3.2%。
- **冷启动用户获益最大**：其信号主要来自餐厅订单历史（而非搜索查询），AUC-ROC提升4.0%，MRR提升1.1%，验证了历史口味偏好可成功跨垂直迁移。
- **高活跃用户获益更多来自搜索查询信号**：其AUC-ROC提升5.2%，MRR提升2.2%，说明短期意图信号对此类用户更有效。
- **特征质量**：搜索查询信号生成的偏好个性化程度高于餐厅订单（搜索意图更明确），但二者均能有效增加推荐相关性。
- **成本可控**：通过精心设计的提示、模型选择和缓存策略，可将LLM推理成本降低约80%，使得该方案在工业级部署中切实可行。

## 7. 优点：方法或实验设计上的亮点

- **创新性强**：首次将分层RAG与LLM生成式特征结合，用于解决工业级多垂直电商的冷启动跨域推荐问题，而非简单使用LLM做零样本排序。
- **工程实用性高**：提出的成本优化策略（提示缓存、即时物化、小模型选择）使得LLM能在高吞吐生产环境中落地，具有很好的现实可操作性。
- **评估严谨**：同时进行离线大范围评估（含人群消融）、在线影子部署和人工/LLM特征质量评分，结果间相互验证，结论可靠。
- **贡献明确**：清晰阐述了如何将LLM生成的非结构化文本转化为结构化特征并集成到现有MTL排序模型中，提供了可复现的范式。
- **视角全面**：覆盖长期偏好（餐厅订单）和短期意图（搜索查询），两种信号互补，符合实际用户行为模式。

## 8. 不足与局限

- **实验覆盖有限**：仅与自身的生产基线对比，未与其它跨域知识转移方法（如多任务embedding共享、图神经网络跨域推荐、传统统计迁移等）对比，难以确定LLM方法相对替代方案的边际增益。
- **在线评估方式**：采用影子部署而非真正在线A/B实验，无法测量对业务指标（如转化率、收入）的直接影响，仅能验证排序质量提升。
- **依赖LLM提示质量与分类体系**：特征生成质量高度依赖人工设计的提示模板和预定义分类树（L1-L4）。分类颗粒度和结构在不同平台可能不同，通用性受限。若分类体系变更，需重新设计提示。
- **计算成本仍为瓶颈**：尽管降低80%，但每日推理全量用户仍需GPT-4o-mini调用的持续开销。对于更大规模平台或更频繁的更新仍可能昂贵。
- **冷启动用户组MRR提升较小（1.1%）**，表明排序头部位置的改善有限，冷启动问题未完全解决，仅在整体排序上取得增益。
- **时效性处理简单**：仅按时间顺序排列历史，未建模衰减或Session上下文，未来可向更动态的session-aware方向演进。
- **未探索检索阶段**：特征仅注入排序阶段，未用于候选召回，因此对完全无历史的新手用户，召回层面仍可能缺失。
- **偏差风险**：LLM可能从餐厅订单数据中学到不当关联（如某些种族菜系与不相关品类），需人工审查特征质量（论文通过置信度阈值和人工/LLM核查部分缓解，但未完全消除）。

（完）
