---
title: "GraspLLM: Towards Zero-Shot Generalization on Text-Attributed Graphs with LLMs"
title_zh: "GraspLLM: 基于大语言模型实现文本属性图的零样本泛化"
authors: "Hengyi Feng, Zeang Sheng, Meiyi Qiang, Li Yang, Wentao Zhang"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11898v2"
tags: ["query:llm-dm"]
score: 8.0
evidence: LLM用于文本属性图分析，属于大语言模型应用
tldr: 针对文本属性图（TAG）上现有方法跨数据集和任务泛化能力不足的问题，提出了GraspLLM框架。该方法利用冻结的通用嵌入模型统一节点语义表示，通过模体感知对比学习提取与数据集无关的结构信息，并设计最优上下文子图对齐到LLM的标记空间。在多个TAG基准数据集上的实验表明，GraspLLM在零样本场景下显著优于之前基于LLM的方法，展示了强大的跨数据集和跨任务泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11898v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1833, \"height\": 972, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11898v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 684, \"height\": 192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11898v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1778, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11898v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 910, \"height\": 1364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11898v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 907, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11898v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 916, \"height\": 413, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11898v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 707, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1796, \"height\": 1232, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1887, \"height\": 489, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1646, \"height\": 641, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1868, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1869, \"height\": 769, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 882, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 912, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11898v2/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 914, \"height\": 255, \"label\": \"Table\"}]"
motivation: 现有方法在跨图、跨任务泛化上表现不足，且对可迁移结构模式捕获有限，因此需增强图结构与语义理解的结合。
method: 采用冻结通用嵌入模型统一语义空间，通过模体感知对比学习提取结构信息，再用最优上下文子图对齐LLM令牌空间。
result: 在多种TAG数据集上，GraspLLM在零样本场景下显著优于先前LLM方法，展现了强泛化性。
conclusion: GraspLLM有效融合图结构理解与LLM语义能力，实现跨数据集和任务的零样本泛化。
---

## 摘要
文本属性图（TAG）的研究因其在引文网络、电子商务平台、社交媒体和网页等广泛实际数据场景中的应用而备受关注。受大语言模型（LLM）卓越语义理解能力的启发，已有大量尝试将LLM融入TAG。然而，现有方法仍难以在不同图和任务间泛化，其捕获可迁移图结构模式的能力有限。为此，我们提出GraspLLM框架，该框架将图结构理解与LLM的语义理解能力相结合，以增强跨数据集和跨任务的泛化性。具体而言，我们使用冻结的通用嵌入模型将不同图的节点文本表示到统一语义空间，在此基础上，跨多个由模体诱导的邻接矩阵进行模体感知对比学习，以提取与数据集无关的结构信息。然后，通过我们提出的最优上下文子图，为每个目标节点提取上下文最相关的子图，并通过对齐投影器将这些子图与LLM的令牌空间对齐。在涵盖多个领域的TAG基准数据集上的大量实验表明，GraspLLM在零样本场景中尤其优于先前的基于LLM的TAG方法，突出了其在不同数据集和任务上的强泛化能力。我们的代码可在https://github.com/Heinz217/GraspLLM获取。

## Abstract
Research on Text-Attributed Graphs (TAGs) has gained significant attention recently due to its broad applications across various real-world data scenarios, such as citation networks, e-commerce platforms, social media, and web pages. Inspired by the remarkable semantic understanding ability of Large Language Models (LLMs), there have been numerous attempts to integrate LLMs into TAGs. However, existing methods still struggle to generalize across diverse graphs and tasks, and their ability to capture transferable graph structural patterns remains limited. To address this, we introduce the GraspLLM, a framework that combines Graph structural comprehension with semantic understanding prowess of LLMs to enhance the cross-dataset and cross-task generalizability. Specifically, we represent node texts from different graphs in a unified semantic space with a frozen general embedding model, on top of which we perform motif-aware contrastive learning across multiple motif-induced adjacency matrices to extract dataset-agnostic structural information. Then, with our proposed optimal contextual subgraph, we extract the most contextually relevant subgraph for each target node and align these subgraphs to the token space of LLM via an alignment projector. Extensive experiments on TAG benchmark datasets spanning diverse domains reveal that GraspLLM consistently outperforms previous LLM-based methods for TAGs, especially in zero-shot scenarios, highlighting its strong generalizability across different datasets and tasks. Our code is available at https://github.com/Heinz217/GraspLLM.

---

## 论文详细总结（自动生成）

# 论文《GraspLLM: Towards Zero-Shot Generalization on Text-Attributed Graphs with LLMs》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：文本属性图（TAG）广泛存在于引文网络、电子商务、社交媒体和网页等场景，节点带有丰富文本信息。近年来，大语言模型（LLM）被尝试集成到TAG分析中，分为“LLM as Enhancer”（增强GNN特征）和“LLM as Predictor”（将图转为LLM可理解序列）两类。
- **核心问题**：现有方法在**跨数据集和跨任务泛化**上能力不足，主要体现在两个维度：
  - **特征偏差（C1）**：不同TAG数据集的节点特征分布差异巨大，导致模型难以迁移（如TEA-GLM跨域零样本准确率从0.548骤降至0.110）。
  - **结构多样性（C2）**：不同图的拓扑模式（聚类系数、连接密度）差异显著，模型难以捕获可迁移的结构知识。
- **整体含义**：急需一种能够同时理解图结构和文本语义的框架，实现**零样本泛化**，即训练后可直接应用于未见过的图和任务。

## 2. 方法论

### 2.1 核心思想
- 将**图结构理解**与**LLM语义理解**深度融合，通过三个关键阶段将图转化为LLM可处理的紧凑子图表示。

### 2.2 关键技术细节

#### 阶段一：结构信息提取（Structural Information Extraction）
- **统一语义编码**：使用冻结的通用嵌入模型（Qwen3-Embedding-8B）将所有节点文本编码到同一语义空间，消除域间特征偏移。
- **模体感知图自监督学习**：
  - 构建四种图模体（边、三角形、4-环、4-团）对应的邻接矩阵 \(\mathbf{A}^{(i)}\)，反映从弱到强的结构关系。
  - 采用多通道GNN，每个模体独立消息传递：\(\mathbf{H}_i^{(l)} = \sigma(\tilde{\mathbf{A}}^{(i)} \mathbf{H}^{(l-1)} \mathbf{W}_i^{(l)})\)，最终节点表示 \(\mathbf{U} = \sum_i \alpha_i \mathbf{H}_i^{(L)}\)。
  - 使用对比学习（GRACE风格）训练：对同一节点在两种增广视图下的嵌入进行对比，最大化正样本相似度，最小化负样本相似度。损失函数如公式(4)-(5)。

#### 阶段二：最优上下文子图采样（Optimal Contextual Subgraph Sampling）
- **定义上下文相关性** \(R(S)\)：融合**结构相干性**（邻居间余弦相似度）和**中心相关性**（与中心节点余弦相似度），由超参数 \(\beta\) 平衡。
- **定义最优上下文子图**：在深度约束 \(T\) 和节点数上限 \(L\) 下，最大化 \(R(S)\) 的子图。
- **贪婪节点选择算法（Algorithm 1）**：以中心节点出发，对每个邻居深度优先扩展，每一步选择边际增益 \(\Delta R(v|S)\) 最高的节点，并通过阈值 \(\tau\) 及早停止。
- **理论保证**：
  - **定理III.3**：贪婪算法达到 \((1 - 1/e)\) 近似最优值（因为 \(R\) 是单调子模函数）。
  - **定理III.4**：若子图忠实近似最优子图且携带非冗余信息，则注入子图可严格降低预测不确定性（熵减）。

#### 阶段三：对齐微调（Alignment Tuning）
- **对齐投影器**：MLP将子图节点特征投影到LLM令牌空间，减去均值进行归一化（保留幅度）。
- **交叉注意力池化**：使用可学习查询与子图令牌做缩放点积交叉注意力，压缩为固定长度表示，避免过长输入。
- **指令微调**：冻结LLM（默认Vicuna-7B-v1.5），仅优化投影器。使用图3/图4所示的提示模板，最大化答案生成似然。

#### 工程优化（超大图可扩展性）
- 向量化批量处理、GPU邻接矩阵（CSR格式）、多GPU数据并行（8卡）、CUDA-IPC共享嵌入、多中心批处理（K=4），将100k节点的子图采样从>100h降至7.8分钟。

## 3. 实验设计

### 3.1 数据集与场景
- **14个真实世界TAG基准**，覆盖5个领域：
  - 引文：Cora、Citeseer、Pubmed、Arxiv
  - 电子商务：History、Computer、Photo
  - 维基页面：WikiCS
  - 社交：Instagram、Reddit
  - 网页：Cornell、Texas、Washington、Wisconsin
- 节点规模从187到169,343，平均度2.65-36.94，全局聚类系数0.01-0.26，体现结构异质性。

### 3.2 对比方法（7类基线）
| 类别 | 代表方法 |
|------|----------|
| MLP | MLP |
| GNNs | GCN, GAT, GraphSAGE |
| Graph Transformers | NodeFormer, DIFFormer |
| Small LMs | BERT, RoBERTa, E5, Sentence-Bert |
| LLMs | Qwen2-7B, LLaMA-2/3.1, Vicuna |
| LLM as Enhancers | OFA, ZeroG, GraphCLIP, UniGLM, LLM-BP |
| LLM as Predictors | GraphGPT, LLaGA, TEA-GLM, GOFA |

### 3.3 实验设置
- **零样本域内**：在Arxiv、Computer、Reddit、Wisconsin上训练，在同类域其他数据集上测试。
- **零样本跨域**：在Arxiv/Reddit/Computer上训练，测试不同域数据集。
- **零样本跨任务**：节点分类模型直接用于链接预测（测试AUC）。
- **监督学习**：完整训练下对比。
- 默认LLM骨干：Vicuna-7B-v1.5，额外验证Mistral-7B、LLaMA-3.1-8B、Qwen3-8B。

## 4. 资源与算力

- **硬件**：8×NVIDIA H20 GPUs，CUDA 12.8。
- **训练时长**：
  - 在Arxiv数据集上，GraspLLM训练约7.3-7.6小时（Fig.8），与LLaGA、TEA-GLM相当。
  - 在OGBN-Products大规模图上：
    - 单GPU GNN推理0.91s（100k节点），子图采样23min，LLM微调14.1h。
    - 8GPU优化后：子图采样7.8min，LLM微调2.4h。
- **内存**：
  - 单GPU子图采样峰值25.2GB，LLM微调47.4GB。
  - 8GPU CUDA-IPC共享嵌入后，主机卡33.9GB，从机卡9.8GB。

## 5. 实验数量与充分性

- **实验组数**：10+组主要实验，涵盖：
  - 零样本域内（表II，10个数据集）
  - 零样本跨域（表III，10个源-目标对）
  - 零样本跨任务（表IV，10个数据集）
  - 监督学习（表VI，10个数据集）
  - 消融研究（表V，2个关键组件，5个数据集×2任务）
  - 子图采样策略对比（Fig.5，5个数据集×2任务）
  - 参数敏感性（Fig.7，β和τ，3个数据集）
  - 提示鲁棒性（表VII，3种格式，5个数据集）
  - 效率分析（Fig.8，对比3方法）
  - 可扩展性（表VIII-IX，OGBN-Products，多种配置）
- **充分性评价**：实验设计全面，覆盖了多种零样本难度、监督对比、消融验证和理论分析；与主流方法公平比较；但弱点在于：**无统计显著性检验**（如置信区间或p值），且无超参数搜索细节，可能影响结果复现。

## 6. 主要结论与发现

- **零样本能力大幅领先**：在域内、跨域、跨任务零样本设置下，GraspLLM在所有14个数据集上平均准确率/ AUC显著超过之前最好方法（如LLM-BP、TEA-GLM）。
- **跨域转移不均衡**：结构相似的数据集（如引文之间）转移损失小，而异质性大的域（如社交→引文）仍有显著下降。
- **监督表现同样优秀**：在监督设置下，GraspLLM取得SOTA或接近SOTA，克服了之前方法“零样本好则监督差”的困境。
- **子图采样有效**：最优上下文子图优于简单的邻域/随机游走，同时理论保证1-1/e近似和信息增益。
- **工程优化实用**：通过GPU并行、批量处理等，可处理百万节点图，采样时间从>100h降至分钟级。

## 7. 优点

- **方法创新性**：
  - 模体感知对比学习：利用多种图模式提取数据集无关的结构先验。
  - 最优上下文子图：结合语义和结构相关性，且有子模性和信息论理论支持。
  - 跨骨干兼容：可适配Vicuna、Mistral、LLaMA、Qwen等多种LLM。
- **实验设计亮点**：
  - 覆盖最全的TAG基准（14个数据集），跨域和跨任务评估非常严格。
  - 消融研究清晰验证了结构提取器和对齐投影器的重要性。
  - 可扩展性详细分析，包括多级工程优化，对实际部署有参考价值。
- **理论贡献**：给出了子图采样的近似比和熵减定理，将启发式策略注入理论基础。
- **实用性**：代码开源，工程优化描述完整，易于复现和大规模应用。

## 8. 不足与局限

- **实验覆盖限制**：
  - 仅评估节点分类和链接预测，未涵盖图级分类、社区检测等任务。
  - 仅处理文本属性图，未推广到异构图、符号图、时序图。
  - 未在更多异构性强的域（如金融、生物）上验证。
- **方法局限**：
  - 子图采样仍会丢失部分信息（线性化为令牌序列），论文承认这是损失不可避免。
  - 跨域转移在结构差异大的对（如社交→引文）下降明显，说明结构分布偏移仍是挑战。
  - 监督结果在弱标签对齐图（如二分类Instagram）上优势缩小。
- **实验公平性潜在风险**：
  - 基准方法未统一使用相同文本编码器（GraspLLM用了Qwen3-Embedding-8B），可能引入不公平优势。
  - 未报告多次运行结果的方差或置信区间，统计稳健性存疑。
  - 提示模板对结果影响显著（表VII），但未探索更多模板。
- **资源消耗**：需要8×H20 GPU，对中小团队门槛仍高；但论文提供了可扩展优化，一定程度缓解。

（完）
