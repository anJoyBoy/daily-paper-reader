---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: AutoSpec：通过归纳逻辑编程实现LLM智能体的安全规则演化
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v2"
tags: ["query:chemical-llm"]
score: 8.0
evidence: 为LLM代理自动演化安全规则
tldr: "LLM代理执行任务时存在安全风险，手工规则易产生误报或漏报，神经方法缺乏可解释性。AutoSpec利用归纳逻辑编程（ILP）引导的反例驱动归纳合成（CEGIS），从专家规则和用户标注中自动演进安全规则。在代码执行和具身代理领域，规则F1分别达0.98和0.93，误报降低94%，仅需4-5次迭代收敛。生成的规则可读、可审计，并能泛化到未见场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 558, \"height\": 289, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: 解决LLM代理安全规则人工制定过程中的保守与宽松矛盾，实现可解释、低误报且高召回的安全规则自动演进。
method: 使用ILP高效识别区分正反例的谓词，剪枝搜索空间，通过CEGIS迭代生成并验证规则候选编辑。
result: "在两领域F1达0.98和0.93，误报降低94%，4-5次迭代收敛，F1比启发式CEGIS高4.8倍。"
conclusion: 自动演进的ILP引导安全规则在精度和召回间取得平衡，可解释且泛化性强，优于传统方法。
---

## 摘要
大语言模型（LLM）智能体通过将语言模型与外部工具和环境集成，日益自动化执行复杂任务。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有的安全方法面临一个根本性的权衡：手工制定的规则是可解释的但脆弱，过度保守的规则会阻止安全操作（高误报率），而宽松的规则则会遗漏不安全行为（高漏报率）。神经分类器缺乏安全关键部署所需的可解释性。我们提出了AutoSpec，这是一个通过反例引导的归纳合成（CEGIS）和归纳逻辑编程（ILP）指导，从用户的安全/不安全标注中自动演化已部署专家设计的安全规则的框架。从专家规则和带标注的轨迹流开始，AutoSpec迭代地评估规则，挖掘误报和漏报反例，使用ILP学习哪些谓词能够区分它们，生成候选规则编辑，并验证候选规则以选择最佳修订。关键洞察在于，ILP能够高效识别那些在漏报中频繁出现但在误报中很少出现的谓词（反之亦然），从而大幅剪枝规则编辑的指数级搜索空间。此过程持续直至收敛，产生平衡精确率和召回率的可解释规则。我们在涵盖代码执行和具身智能体领域的291条执行轨迹上评估了AutoSpec。AutoSpec在两个领域将规则F1提升至0.98和0.93，实现了高达94%的误报减少，同时保持高召回率，并在4-5次迭代内收敛。ILP引导的方法比启发式CEGIS实现了高达4.8倍的F1提升。学习到的规则是人类可读、可审计的，并且能够泛化到未见场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

# AutoSpec：通过归纳逻辑编程实现LLM智能体安全规则的自动演化

## 1. 论文的核心问题与整体含义（研究动机和背景）

LLM智能体（如代码执行、机器人控制）在执行任务时面临严重的安全风险：可能执行破坏性命令、泄露敏感数据或违反领域约束。现有安全方案存在两难：**手工规则**可解释但脆弱——过度保守导致高误报（干扰合法操作），过度宽松导致高漏报（遗漏危险行为）；**神经分类器**（如基于LLM的判别器）具备适应性但缺乏可解释性，在安全关键场景中不可审计、难以信任。**核心问题**是：能否利用智能体运行过程中积累的用户安全/不安全标注，自动演化已部署的专家安全规则，使其保持可解释性的同时提升性能？

论文将规则维护重定义为**反例引导的归纳合成（CEGIS）问题**：用户标注作为反例（误报/漏报），通过迭代合成、验证、反例反馈来改进规则。其关键洞察是：**归纳逻辑编程（ILP）能够高效识别区分正反例的谓词**，从而大幅剪枝规则编辑的指数级搜索空间。

整体意义：首次实现了从用户反馈中自动演化符号化安全规则的工具，使规则层兼具可解释性和自适应性，可部署于防御纵深架构中。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
将安全规则演化建模为 **ILP引导的CEGIS循环**。初始专家规则（R₀）、带标注的执行轨迹（D）、谓词库（P）作为输入。每个迭代包含四个阶段：
1. **反例挖掘**：评估当前规则，收集误报（FP）和漏报（FN）轨迹。
2. **ILP谓词学习**：将FN作为正例、FP作为负例，使用ILASP求解器学习最小谓词组合（析取范式）来区分它们。输出的谓词带有方向（召回提升或精确率提升）。
3. **候选生成与选择**：基于ILP建议，通过四种编辑操作（添加合取、添加例外、添加析取分支、放宽）生成候选规则。候选通过混合修复链、失败驱动分支等策略构建，并在完整数据集上评估，选取得分最高的（F1 > 精确率 > 未解决反例数 > 规则复杂度的词典序）。
4. **验证与收敛**：若候选优于当前规则则接受，否则停止。附加多样性压力避免重复建议。循环直至达到目标阈值、迭代次数用尽或无法改进。

### 关键技术细节
- **谓词库**：跨领域设计（代码执行领域16-24个谓词，如`destructive_os_inst`, `is_temp_file`；具身领域如`near_danger_zone`, `carrying_liquid`）。评估是事件级别的，标注是轨迹级别的。
- **编辑操作**：表格2定义了四种：AddConjunct（加合取）、AddException（加否定）、AddDisjunct（加析取分支）、Relax（放宽）。这些操作可组合。
- **ILP学习**：使用ILASP v4.4.1，设定120秒超时。学习的是最小谓词组合（简单性正则），避免过拟合少量反例。
- **候选评分公式**：词典序：F1 > Precision > -U（未解决反例数） > -|R'|（规则数）。
- **收敛保证**：单调得分改进（因为贪心接受）、局部最优性、确定性输出。但整体问题NP难，不保证全局最优。

### 算法流程（伪代码说明）
输入：R₀, D_L, P, 最大迭代K, 目标分数θ
输出：R*
1. R = R₀
2. 对 i=1..K:
   - 若 S(R) ≥ θ 则返回R
   - CE = MineCounterexamples(R, D_L)
   - 若 CE=∅ 则返回R
   - suggestions = LearnILP(CE, D_L, P)
   - candidates = CandidateGen(R, CE, suggestions)
   - best = SelectBest(candidates, D_L)
   - 若 S(best) > S(R) 则 R = best，否则返回R
3. 返回R

## 3. 实验设计

### 数据集与场景
- **代码执行领域**：191条轨迹（91不安全来自RedCode-Exec，100安全由GPT-5.1-Codex生成）。覆盖25个风险类别。轨迹长度1-9事件（均值1.4）。
- **具身智能体领域（家庭机器人）**：100条轨迹（52%不安全），来自SafeAgentBench（AI2-THOR环境）。机器人执行任务如“从桌上取笔记本”。轨迹长度1-15事件（均值5.1）。
- 所有轨迹逐条标注为安全/不安全（轨迹粒度）。

### 评估指标
- 主要指标：F1、精确率、召回率（轨迹粒度，规则触发任何事件即预测不安全）。
- 辅助：收敛迭代次数、规则复杂度、候选效率。

### 对比方法（baselines）
- **B1：专家设计规则（AgentSpec）**：人工编写优化的规则集，是演化的起点和参考。
- **B2：LLM分类器**：使用相同标注作为few-shot示例（每类5条）进行安全分类，替代规则方法。
- **B3：无ILP的CEGIS**：移除ILP引导，仅用覆盖统计排序谓词。
- **B4：随机搜索**：固定候选评估预算下随机采样。

### 实验协议
- RQ1（效果比较）：在70%种子分裂上演化规则，在30%留出测试集上评估。B1直接评估。
- RQ2（效率）：报告种子上内样本收敛轨迹。代码域从单谓词种子开始（F1≈0），具身域从专家规则开始（F1=0.493）。
- RQ3（泛化）：随机70%初始任务用于学习，30%新任务用于评估泛化。

## 4. 资源与算力

论文中**未明确说明训练使用了GPU型号、数量或训练时长**。所有实验在MacBook Pro（M2 Max，32GB RAM）上运行。ILASP设置120秒超时。整个AutoSpec运行过程（包括ILP求解、候选评估）约需数十秒（代码执行20.1s，具身11.8s）。因此可以认为算力需求较低，不需要大规模GPU服务器。

## 5. 实验数量与充分性

### 实验组数
- 两个领域共4组基准对比（B1-B4+AutoSpec），每组报告精确率、召回率、F1、FP/FN计数。
- 收敛曲线（两个领域各一张图，5次迭代）。
- ILP vs 启发式消融（两个领域各一组，报告迭代数、最终F1、候选数、时间）。
- 泛化实验（两个领域，初始/新任务对比图，包括F1、精确率、召回率）。
- 用户研究（10名实践者，4个场景，120次成对比较，3维度评分）。
- 消融实验（代码域：全系统 vs 去掉ILP引导 vs 去掉失败驱动分支 vs 去掉多步编辑）。

### 充分性与公平性
- **充分**：涵盖了主要对比、消融、泛化、用户评估。实验设计考虑了种子分裂、独立测试集，避免过拟合。
- **公平**：所有方法使用相同标注数据和谓词库。对比方法（B3/B4）与AutoSpec保持相同预算（迭代数/候选数）。B2使用LLM分类器但未调优，可能略弱（论文承认并非声称优于所有LLM分类器）。用户研究中对比的是AutoSpec规则 vs LLM分类器决策，这是合理的（因为两者都利用反馈）。
- **不足**：实验仅在两个领域（代码执行和家庭机器人）进行，未扩展到网络代理、数据库代理等。每领域轨迹数量中等（191/100），统计显著性可能有限。用户研究仅有10名参与者。

## 6. 论文的主要结论与发现

- **效果**：AutoSpec在两个领域达到F1=0.98（代码）和0.93（具身），相比专家规则（B1）分别提升40.4%和89.2%。误报降低94%（代码：31→2 FP）。
- **效率**：4-5次迭代收敛。ILP引导比启发式CEGIS（B3）的F1高4.8倍（代码：0.98 vs 0.203）。ILP开销适度（20.1s vs 4.4s），但质量收益显著。
- **泛化**：学习到的规则从70%初始任务泛化到30%新任务，F1仅下降1.6%（代码）和4.7%（具身），证明未过拟合。
- **可解释性与信任**：用户研究中，AutoSpec规则在可解释性（4.97/5）、可操作性（4.83/5）、信任度（4.80/5）上远优于LLM分类器（3.45/2.73/2.77）。实践者偏好AutoSpec的84.2%成对比较。
- **消融**：ILP引导是最关键组件（去掉后F1从0.98降至0.20）。多步编辑和失败驱动分支也贡献显著。

## 7. 优点

- **问题定义清晰**：将规则维护重新定义为自动演化问题，使用用户标注信号，贴合实际部署场景。
- **方法新颖有效**：组合ILP与CEGIS，利用ILP的学习能力剪枝搜索空间，避免启发式搜索的低效。编辑操作设计简洁，覆盖了主要的规则调整类型。
- **可解释性优先**：最终规则是人类可读的符号语言，支持审计和手动调整，这是神经方法无法提供的。
- **实验全面**：包括效果、效率、泛化、消融、用户研究，验证了设计的每个部分。
- **实际部署导向**：AutoSpec可异步运行，不增加用户等待时间；规则可直接部署于现有规则引擎（如AgentSpec）。
- **开源实现**：代码12K行，基于AgentSpec，使用ILASP作为ILP后端。

## 8. 不足与局限

- **领域覆盖有限**：仅在代码执行和家庭机器人两个领域验证。Web代理、数据库代理、自动驾驶等其他高风险场景未测试。
- **数据集规模较小**：191+100条轨迹，可能不足以反映真实世界分布的多样性。安全和不安全轨迹的比例可能不均衡。
- **谓词库依赖**：ILP学习的有效性依赖于谓词库的丰富性和质量。如果关键安全特征未被谓词捕获，系统可能无法学习到有效规则。论文承认这一点，并说明未解决的反例可指导添加新谓词，但未实验证明。
- **全局最优不保证**：算法NP难，贪心策略可能陷入局部最优。论文仅保证局部单调改进。
- **用户研究样本量小**：10名参与者，虽然统计显著（Wilcoxon p<0.01），但结论的普适性可进一步加强。
- **时间属性处理**：当前仅基于事件局部属性，不支持时态逻辑（如动作序列）。论文提到可作为未来扩展，但当前实现不处理状态依赖行为。
- **LLM分类器对比可能不公**：B2使用简单的few-shot提示，不是最先进的微调分类器（如Llama Guard）。论文也承认这一点，称目的不是超越所有LLM分类器，而是表明符号规则的可解释性优势在同一反馈信号下。

（完）
