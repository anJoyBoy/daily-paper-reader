---
title: "ZAS-SQL: Distilling Rules from Failures for Zero-Shot Text-to-SQL"
title_zh: "ZAS-SQL: 从失败中提炼规则用于零样本文本到SQL"
authors: "Hongzhou Zheng, Yixin Gou, Wenjia Zhang"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08245v1"
tags: ["query:llm-dm"]
score: 9.0
evidence: 利用大语言模型解决文本到SQL转换这一数据挖掘任务
tldr: "文本到SQL任务中，零样本方法因缺乏有效约束而落后于少样本。本文提出ZAS-SQL框架，通过Map-Reduce从失败案例中蒸馏核心生成规则，并结合知识增强的模式表示、规则驱动的结构化推理和执行引导早停三个模块。在Spider上达87.2%和88.6%执行准确率，超越GPT-4/4o；在UrbanPlan上达81.3%，验证跨域泛化能力。该框架即使使用4B小模型也优于闭源模型零样本基线，展现了强模型通用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1596, \"height\": 909, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1656, \"height\": 733, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 782, \"height\": 415, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 1108, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1212, \"height\": 415, \"label\": \"Table\"}]"
motivation: 零样本Text-to-SQL中LLM失败呈现系统模式，可从中提取规则以弥补缺乏生成约束的不足。
method: 通过Map-Reduce管道从失败案例蒸馏规则，辅以知识增强模式表示、规则驱动推理和执行引导早停。
result: "Spider开发集/测试集87.2%/88.6%，UrbanPlan 81.3%，零样本新SOTA，超越GPT-4/4o少样本方法。"
conclusion: 规则蒸馏方法泛化至新领域，小模型也超越闭源大模型零样本，展示通用有效性。
---

## 摘要
文本到SQL将自然语言转换为可执行的SQL查询。基于大型语言模型（LLM）的少样本上下文学习方法表现出色，但它们依赖于示例，限制了跨领域泛化并消耗大量上下文窗口空间。现有的零样本方法缺乏有效的生成约束，性能仍不及少样本方法。我们观察到，LLM在零样本文本到SQL中的失败并非随机，而是呈现出系统性的、重复出现的模式。基于这一观察，我们提出了一种完全零样的文本到SQL框架，该框架通过基于Map-Reduce的规则提炼管道从失败案例中提取核心生成规则，并通过三个互补模块提高生成质量：知识增强的模式表示（补充数据定义语言中缺失的语义）、规则驱动的结构化推理框架（抑制结构偏差）以及执行引导的早停机制（实现低成本的自纠正）。在Spider数据集上，所提框架在开发集和测试集上分别达到了87.2%和88.6%的执行准确率，建立了新的零样本最先进水平，并超越了多个基于GPT-4/4o的少样本和微调方法。在特定领域数据集UrbanPlan上，它达到了81.3%，证实了规则提炼方法在不同领域的泛化能力。此外，当配备4B参数模型时，该框架超越了领先闭源模型的零样本基线，显示出强大的模型通用性。

## Abstract
Text-to-SQL translates natural language into executable SQL queries. Few-shot in-context learning methods built upon large language models (LLMs) achieve strong performance, yet their reliance on demonstrations limits cross-domain generalization and consumes substantial context window space. Existing zero-shot methods, lacking effective generation constraints, still fall short of few-shot approaches. We observe that LLM failures in zero-shot Text-to-SQL are not random but exhibit systematic, recurring patterns. Building on this observation, we propose a fully zero-shot Text-to-SQL framework that distills core generation rules from failure cases through a Map-Reduce-based rule distillation pipeline and improves generation quality via three complementary modules: knowledge-augmented schema representation, which supplements missing semantics in Data Definition Language; a rule-driven structured reasoning framework that suppresses structural deviations; and Execution-Guided Early Stopping, which enables low-cost self-correction. On Spider, the proposed framework achieves up to 87.2% and 88.6% execution accuracy on the Dev and Test sets, respectively, establishing a new zero-shot state-of-the-art and surpassing multiple few-shot and fine-tuning methods built upon GPT-4/4o. On the domain-specific dataset UrbanPlan, it achieves 81.3%, confirming that the rule distillation approach generalizes across domains. Moreover, when equipped with a 4B-parameter model, the framework surpasses zero-shot baselines of leading closed-source models, demonstrating strong model generality.

---

## 论文详细总结（自动生成）

# 论文《ZAS-SQL: Distilling Rules from Failures for Zero-Shot Text-to-SQL》详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：零样本（Zero-shot）Text-to-SQL 任务中，大语言模型（LLM）因缺乏有效的生成约束，性能显著低于依赖标注示例的少样本（Few-shot）方法。
- **观察**：LLM 在零样本下的失败并非随机，而是呈现出系统性的、可重复的模式（如过度复杂化、模式语义缺失、单次推理脆弱性）。
- **动机**：从零样本失败案例中自动提取可复用的生成规则，以弥补零样本场景下的约束缺失，从而实现无需标注数据的跨领域泛化。

## 2. 方法论
### 核心思想
提出 **ZAS-SQL** 框架，通过“从失败中蒸馏规则”来替代少样本中的显式示例，并结合三种互补模块提升生成质量。

### 关键技术细节
1. **可迁移规则蒸馏管道（Map-Reduce + HITL）**  
   - **局部模式提取**：对每个失败案例（共1725个），让LLM从错误类型、推理层级、失败原因、缺失信息四个维度生成结构化报告。  
   - **全局模式聚合**：通过LLM对局部报告进行聚合与去重，得到20个全局错误模式。  
   - **回溯对齐**：用20个全局模式重新标注所有案例，对齐率99.6%。  
   - **核心规则推导**：由两名领域专家将20个全局模式抽象为8条可操作的生成规则（如值匹配/精度、列语义消歧、JOIN策略、集合操作、SQLite数据类型等）。

2. **知识增强的模式表示（Knowledge-Augmented Schema S+）**  
   - 离线预处理：对每个列随机采样10个去重值，并让LLM自动推断自然语言语义描述。  
   - 将原始 DDL、语义描述、采样值拼接为 S+，以弥补 DDL 中缺失的业务语义与值分布，减少模式链接错误。  
   - 零人工标注，零推理开销。

3. **规则驱动的结构化推理框架（Structured Reasoning）**  
   - 将8条核心规则（G）和五步推理框架（F）注入提示中。  
   - 推理步骤：意图分解 → 模式定位 → 结构决策（JOIN/子查询） → 条件与排序逻辑 → 生成与自验证（对照规则检查）。  
   - 每个步骤嵌入针对高频错误的检查指令，显式约束生成边界。

4. **执行引导早停（Execution-Guided Early Stopping, EGES）**  
   - 多路径采样（温度 τ=1，最大采样数 N_max=20），每轮候选 SQL 在数据库上执行得到结果集。  
   - 按结果集等价类分组（忽略语法/执行错误），当某一等价类的大小 ≥ 阈值 θ=6 时立即停止采样。  
   - 最终输出该等价类中最短的 SQL（字符数最少）。  
   - 优点：避免固定采样次数带来的 token 浪费，防止错误级联，不需要额外训练。

### 整体提示结构
```
P = Concat(G, S+, q, F)
```
其中 G 为核心规则，S+ 为增强模式，q 为自然语言问题，F 为推理框架。

## 3. 实验设计
### 数据集
- **Spider**：跨领域基准，200个数据库，138个领域，11480个问题-SQL对，分为 Easy/Medium/Hard/Extra 四级。官方开发集1034例，测试集2147例。
- **UrbanPlan**：域特定数据集，来自某城市规划院真实工作流，包含375对问题-SQL，覆盖3个子数据库、19张表，场景包括人口迁移、通勤模式、住房市场等。150例用于规则蒸馏，225例用于评估。

### 对比方法
- **零样本基线**：ChatGPT-SQL、C3-SQL、Alpha-SQL。
- **少样本 ICL**：DIN-SQL、DAIL-SQL、ACT-SQL、DEA-SQL、MCS-SQL、MAC-SQL、OpenSearch-SQL。
- **监督微调**：SENSE-SQL、DTS-SQL、ROUTE-SQL。

### 评估指标
执行准确率（Execution Accuracy, EX），比较执行结果集（而非 SQL 文本），使用官方 Spider 评估套件。

### 模型与设置
- 主要 backbone：DeepSeek-V3（开源，关闭思考模式）。
- 多模型验证：GPT-4o、GLM-5、Kimi-K2.5、Qwen3-4B/8B、Qwen2.5-3B/14B/Coder-14B/72B 等。
- 超参数：采样温度 τ=1，最大采样数 N_max=20，早停阈值 θ=6。

## 4. 资源与算力
- 文本明确说明：所有本地模型部署在 **8×A800 GPU** 上。商业模型通过 API 访问。
- 未提及训练时长（零样本方法无需训练，仅需推理和预计算；规则蒸馏中的 LLM 调用及 HITL 耗时未量化）。

## 5. 实验数量与充分性
- **主要实验**：Spider Dev/Test 对比（表1），UrbanPlan 对比（表2），多模型零样本基线（15个模型，表1下方文字），消融实验（表3）。
- **消融实验**：在 DeepSeek-V3 上逐步添加五个模块（模式描述、值示例、SQL规则、分步推理、EGES），报告每个步骤的增益（表3）。
- **域迁移验证**：UrbanPlan 作为补充，证明管道可迁移且规则集体现领域适应（包含窗口函数、CTE 等特定规则）。
- **充分性**：实验覆盖了多个数据集、多个 backbone、多种范式对比，消融设计合理。但未报告多次运行的标准差或统计显著性检验。对比方法均引用自原文，未进行额外复现。

## 6. 主要结论与发现
- **零样本 SOTA**：在 Spider Dev/Test 上分别达到 **87.2%/87.8%**（DeepSeek-V3），超越所有零样本方法，并超过多个 GPT-4/4o 的少样本和微调方法。
- **小模型有效**：Qwen3-4B 在 Spider Test 上达到 **86.3%**，优于不少大模型的零样本基线，但 Qwen2.5-3B 仅 69.7%，表明存在能力下限。
- **跨领域泛化**：UrbanPlan 上达 **81.3%**，且相对增益比 Spider 更大，说明规则约束在复杂域特定查询中作用更显著。
- **规则贡献最大**：消融显示，“SQL生成规则”模块带来了最大的单步提升（Dev +4.5%，Test +6.7%，UrbanPlan +5.4%），占总增益的 45%~53%。
- **知识增强模式**：语义描述（尤其对域特定列）贡献显著（Dev +2.1%，UrbanPlan +3.6%）；值示例在数值型为主的 UrbanPlan 中增益有限（+0.4%）。
- **EGES 有效**：基于执行结果等价类的早停机制在已强化的管道上仍能带来额外 1.0%-1.5% 的增益。

## 7. 优点
- **创新性**：首次系统性地从零样本失败案例中蒸馏可传递的生成规则，将失败模式转化为显式约束，替代少样本中的示例。
- **模块化与实用性**：三个模块均零训练、零人工标注（除 HITL 外），规则蒸馏管道在其他领域也可复用，具有实际部署可行性。
- **强泛化性**：在跨领域（Spider）和域特定（UrbanPlan）上都取得 SOTA，证实方法通用。
- **资源效率**：小模型也能达到有竞争力性能；EGES 减少采样次数，降低推理成本。
- **实验设计规范**：消融实验逐步累积，每步贡献可量化；多 backbone 验证排除了模型选择偏差。

## 8. 不足与局限
- **基准覆盖有限**：仅在 SQLite 上验证，未测试企业级数据库（Oracle、PostgreSQL）。BIRD 等引入外部知识的数据集尚未评估。
- **EGES 开销**：当单条 SQL 执行时间较长时，验证开销仍不可忽视；阈值 θ 为经验设定，可能需针对不同难度分布调整。
- **语义描述质量**：自动生成的语义描述未经系统验证，在高度专业化领域（如医疗、法律）可能不准确，引入噪声。
- **规则域迁移成本**：虽然管道可迁移，但 HITL 抽象步骤（两名专家将20个全局模式转变为8条规则）尚未自动化，仍依赖人工。
- **数据可用性**：UrbanPlan 数据集基于合作机构专有数据库，无法公开，影响复现。
- **统计显著性未报告**：未说明多次运行的标准差或置信区间，对于小模型结果（如Qwen3-4B 86.3%）的稳定性尚需确认。

（完）
