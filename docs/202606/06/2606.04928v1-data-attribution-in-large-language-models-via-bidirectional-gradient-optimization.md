---
title: Data Attribution in Large Language Models via Bidirectional Gradient Optimization
title_zh: 大型语言模型中通过双向梯度优化的数据归因
authors: "Frédéric Berdoz, Luca A. Lanzendörfer, Kaan Bayraktar, Roger Wattenhofer"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04928v1"
tags: ["query:llm-dm"]
score: 8.0
evidence: LLM训练数据归属作为数据挖掘技术
tldr: 大语言模型训练数据归因是模型治理的关键难题。本文提出双向梯度优化方法，对生成文本同时进行梯度上升和下降，测量训练样本损失变化，实现任意粒度的数据归因。在已知数据集上的实验表明，该方法在影响指标上优于现有基线。该工作增强了模型可解释性，为负责任的AI提供有力支撑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04928v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04928v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 661, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04928v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1842, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1841, \"height\": 1154, \"label\": \"Table\"}]"
motivation: 归因训练数据对模型输出的影响，以提升大语言模型的可解释性和问责性。
method: 对生成文本进行双向梯度优化（梯度上升和下降），计算训练样本损失变化，支持事实与风格归因。
result: 在预训练模型上，该方法在影响指标上优于基线，能有效追溯关键训练数据。
conclusion: 双向梯度优化为LLM数据归因提供了有效框架，促进了可解释AI的发展。
---

## 摘要
大语言模型（LLMs）正越来越多地被部署在各种应用中，给治理、问责和数据溯源带来了关键问题。理解哪些训练数据对模型输出影响最大仍然是一个基本未解决的问题。我们通过针对自回归大语言模型的训练数据归因（TDA）来解决这一挑战，基于逆公式的扩展：如果模型在训练期间看到了生成的输出，训练数据会如何受到影响？我们的方法通过对生成的文本样本使用双向梯度优化（梯度上升和下降）来扰动基础模型，并测量训练样本上损失的变化。我们的框架支持任意数据粒度的归因，能够实现事实归因和风格归因。我们在已知数据集的预训练模型上对我们的方法进行了基线评估，并表明它在影响度量上优于先前的工作，从而增强了模型的可解释性，这是负责任AI系统的关键要求。

## Abstract
Large Language Models (LLMs) are increasingly deployed across diverse applications, raising critical questions for governance, accountability, and data provenance. Understanding which training data most influenced a model's output remains a fundamental open problem. We address this challenge through training data attribution (TDA) for auto-regressive LLMs by expanding upon the inverse formulation: How would training data be affected if the model had seen the generated output during training? Our method perturbs the base model using bidirectional gradient optimization (gradient ascent and descent) on a generated text sample and measures the resulting change in loss across training samples. Our framework supports attribution at arbitrary data granularity, enabling both factual and stylistic attribution. We evaluate our method against baselines on pretrained models with known datasets, and show that it outperforms previous work on influence metrics, thereby enhancing model interpretability, an essential requirement for accountable AI systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）输出的生成过程受到训练数据的影响，但**无法直接追溯**哪些训练样本对特定输出起了关键作用。这一“逆问题”是模型可解释性和AI问责制的基础难题。
- **研究动机**：当前LLM广泛应用（法律、医疗、科研），但其“黑箱”特性使得数据来源、模型偏差难以审计。若能有效归因训练数据，可实现模型调试、数据估值、机器遗忘等下游应用，提升AI系统的可治理性。
- **整体含义**：本文提出一种**基于双向梯度优化的训练数据归因（TDA）框架**，通过测量训练样本在梯度上升/下降模型上的损失变化来评估其对生成文本的贡献，从而在任意数据粒度（样本级、令牌级）上实现事实和风格归因。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：基于**镜像影响假设**（mirrored influence hypothesis）：训练样本对模型输出的影响，可以用该输出对训练样本的反向影响来近似。具体地，对生成文本 \( \hat{x} \) 分别做梯度上升（模拟“遗忘”）和梯度下降（模拟“强化”），得到两个扰动模型 \( \theta_{\hat{x}}^+ \) 和 \( \theta_{\hat{x}}^- \)；然后计算每个训练样本 \( x_i \) 在这两个模型下的损失之差绝对值，作为**双向影响分数（Bidirectional Influence Score, BIS）**：

  \[
  I(x_i; \hat{x}) = |\ell(x_i, \theta_{\hat{x}}^-) - \ell(x_i, \theta_{\hat{x}}^+)|
  \]

  按BIS降序排列即可得到最影响的训练样本。

- **技术细节**：
  - **模型扰动**：基于预训练权重 \( \theta^* \) 初始化，对生成文本 \( \hat{x} \) 执行 \( M \) 步梯度上升/下降（文中 \( M=10 \)，学习率 \( \alpha=10^{-4} \)）。
  - **正则化**：使用**Fisher信息矩阵（FIM）**的对角近似来防止灾难性遗忘，即每一步更新量为：
    \[
    \theta^{(m+1)}_\pm \leftarrow \theta^{(m)}_\pm \pm \alpha N \hat{F}^{-1}_{\theta^*} \nabla \ell(\hat{x}, \theta)
    \]
    其中 \( \hat{F} \) 是训练数据所有样本梯度平方的均值（对角近似）。
  - **归因粒度**：除样本级外，还可计算各令牌的损失变化实现细粒度解释。

- **算法流程**（文字描述）：
  1. 对给定生成输出 \( \hat{x} \)（由提示生成），从预训练模型 \( \pi_{\theta^*} \) 开始。
  2. 分别使用梯度下降和梯度上升（各 \( M \) 步）更新模型，得到 \( \theta_{\hat{x}}^- \) 和 \( \theta_{\hat{x}}^+ \)。
  3. 遍历所有训练样本 \( x_i \)，计算其损失 \( \ell(x_i, \theta_{\hat{x}}^-) \) 和 \( \ell(x_i, \theta_{\hat{x}}^+) \)。
  4. 计算BIS并排序，输出top-k样本。

## 3. 实验设计

### 3.1 使用数据集与场景

- **事实归因场景**：使用**Wikipedia摘要**（WIT数据集，约2.3亿token），风格统一，迫使归因依赖事实内容。
- **风格归因场景**：使用**Project Gutenberg**文学作品（约200万token），包含多样作者风格，考验风格层面的归因。

两者均基于**GPT-2架构**从头训练（上下文窗口256 token，步长128的滑动窗口切分训练块）。

### 3.2 Benchmark与对比方法

- **评价指标**：**Tail-Patch Absolute (TPA) 分数**——将top-k归因样本对基础模型做一次梯度更新后，测量生成文本似然度的绝对变化比例。同时用**完整重训练**（移除top-k样本后从头训练）作为验证。
- **对比基线**：
  - Random（随机样本）
  - BM25（词频检索）
  - TRACK STAR（基于梯度的可扩展影响函数，Chang et al. 2025）
  - GECKO（基于文本嵌入的检索，Lee et al. 2024）

### 3.3 实验设置

- 生成方式：事实场景用“主语.”提示，Top-50采样（温度1，重复惩罚1.5）；风格场景用文学片段第一句做提示（无重复惩罚）。
- 归因评估：25个查询样本（事实），每个查询取不同top-k（1,3,5,7,10,15,20）计算TPA；风格场景类似。还做了完整重训练验证（4个风格查询，k=20/50/100）。

## 4. 资源与算力

**论文中未明确说明使用的GPU型号、数量及训练时长。** 只提到“GPT-2风格模型从头训练”，未披露训练成本。这可能是由于该工作聚焦方法论验证，且使用的公开数据集（Wikipedia 2.3亿 token，Gutenberg 200万 token）规模有限，计算开销在可控范围内。但作者在局限性中也承认DABGO需要两轮全数据集遍历，对于工业级模型（如Llama 3.1）仍然昂贵。

## 5. 实验数量与充分性

- **实验数量**：共两大类场景（事实、风格），每类含25个查询样本，对比4种基线（+Random），对不同top-k（7个值）做TPA评估；额外做了**消融实验**（仅上升 vs 仅下降 vs 双向）以及**完整重训练验证**（4个查询×3个k值）。
- **充分性**：实验设计较为全面，覆盖了定量指标、消融研究、重训练验证，以及定性示例。但查询样本数量（25个/场景）偏少，重训练仅用4个查询，统计显著性可能不足。此外，仅测试GPT-2规模模型，未扩展到更大参数量的模型。
- **客观公平性**：对比方法均采用超参微调？论文未讨论基线的最佳超参设置，但基线结果（如BM25在事实场景top-1表现突出）符合预期，说明对比较为公平。TPA指标是领域内认可的近似指标，且有完整重训练支持其有效性。

## 6. 主要结论与发现

1. **性能领先**：DABGO在绝大多数top-k设置下TPA分数显著高于所有基线，尤其在事实场景中（除k=1时BM25因词法匹配略优）。
2. **双向优于单向**：消融实验表明，联合梯度上升和下降的效果优于单独使用任一种方向（上升单独在部分k上接近，但双向整体更好）。
3. **风格归因有效**：在Gutenberg风格场景中，DABGO能归因到同一作者的文本片段，且细粒度令牌高亮可解释性更强。
4. **完整性验证**：基于重训练的损失增加验证了DABGO识别的top-k样本确实对生成文本有因果关系，且优于基线。
5. **细粒度解释能力**：令牌级损失差异高亮可定位训练样本中对归因贡献最大的片段，增强可解释性。

## 7. 优点

- **方法创新**：提出双向梯度优化（同时上升/下降）来增强归因信号，相比以往仅用“遗忘”（上升）的方法更全面。
- **无需逐样本梯度**：利用反向影响计算，避免计算全模型Hessian阵或逐训练样本梯度，提高了可扩展性。
- **粒度灵活性**：支持样本级、令牌级甚至任意层次归因。
- **实验验证扎实**：使用TPA与完整重训练双重验证，消融研究明确了双向优劣。
- **代码开源**：提供GitHub仓库，促进可复现性。

## 8. 不足与局限

- **计算成本**：需要两轮遍历全部训练样本计算损失变化（每个查询），对于极大规模语料（数十亿token）在工业场景中不实用。作者也承认这一点。
- **忽略样本间交互**：仅独立评估每个样本的BIS，无法捕获样本组间的协同或对抗影响。
- **token级归因未验证因果性**：令牌级高亮仅基于损失变化，尚未确认其真正的因果角色。
- **实验规模有限**：仅使用GPT-2规模模型和百万级token数据集，未在更大模型（如Llama）上验证，泛化性存疑。
- **查询样本偏少**：事实/风格场景各25个查询，重训练仅4个查询，统计波动可能影响结论稳健性。
- **未讨论基线超参优化**：可能对基线不公，但文中TPA结果趋势合理。

（完）
