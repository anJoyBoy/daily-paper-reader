---
title: Interpretable Inverse Design of Metal-Organic Frameworks with Large Language Model Agents
title_zh: 基于大型语言模型代理的可解释金属有机框架逆向设计
authors: "Kyungmin Nam, Seunghee Han, Jihan Kim"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29459v1"
tags: ["query:chemical-llm"]
score: 9.0
evidence: 利用LLM智能体进行金属有机框架的可解释逆向设计，属于化学工程问题
tldr: MOF逆设计面临组合空间大、标签昂贵且模型不可解释的挑战。LLM4MOF采用语言模型代理闭环框架，一个代理提出化学假设，另一个转化为约束选择候选，通过诊断光束测试验证假设。在六个吸附、分离和电子结构任务中，仅400次评估即聚焦高性能结构，并能从头生成新MOF并模拟验证。该方法无需每目标训练模型，实现可解释、低成本的逆设计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29459v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1605, \"height\": 1482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29459v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1503, \"height\": 1010, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29459v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1534, \"height\": 1991, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29459v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1625, \"height\": 738, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29459v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1525, \"height\": 1163, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29459v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1492, \"height\": 1414, \"label\": \"Figure\"}]"
motivation: 传统MOF逆设计依赖昂贵标签和黑箱模型，缺乏可解释性，LLM4MOF利用语言模型代理实现可解释搜索与迭代优化。
method: 闭环比框架：一个LLM代理提出可解释设计假设，另一个代理转化为约束筛选候选MOF，通过诊断光束测试区分几何、化学或金属影响，经模拟反馈迭代十轮。
result: 在六项任务中，400次评估内搜索到顶级结构；从头生成新MOF并模拟验证，成本约1美元，性能优于随机搜索和遗传算法。
conclusion: 语言模型代理可驱动可解释、模拟扎根的逆设计，无需为每个目标单独训练模型。
---

## 摘要
金属有机框架（MOFs）的逆向设计需要在组合庞大的空间中搜索，其中属性标签昂贵，且大多数机器学习模型对结构为何成功几乎未提供解释。我们提出了LLM4MOF，一个闭环框架，其中语言模型代理推理化学知识、构建候选MOFs并在模拟中测试，通过十次自主迭代细化假设。一个代理提出关于金属节点、连接体、孔隙几何和功能化学的可解释设计假设，另一个代理将其转化为约束条件，选择由金属节点、有机连接体和匹配拓扑组成的候选MOFs。每个假设通过四个诊断束进行测试，这些诊断束应用其约束的不同子集，因此比较它们可以显示是几何、化学还是金属选择驱动了性能。即使对数据库的全局属性景观一无所知，LLM4MOF也能在400次属性评估内，将其搜索集中在六个吸附、分离和电子结构任务中的高性能结构上。同一循环还从头生成新的MOFs，并在实时模拟中验证，根据每个请求条件调整几何形状，其性能优于随机搜索和遗传算法，每次活动成本约为1美元。LLM4MOF表明，语言模型代理可以在无需为每个目标训练模型的情况下，运行基于模拟的可解释逆向设计。

## Abstract
Inverse design of metal-organic frameworks (MOFs) requires searching a combinatorially vast space where property labels are expensive and most machine-learning models reveal little about why a structure succeeds. We introduce LLM4MOF, a closed-loop framework in which language-model agents reason about chemistry, build candidate MOFs, and test them in simulation, refining hypotheses over ten autonomous iterations. One agent proposes interpretable design hypotheses over metal nodes, linkers, pore geometry, and functional chemistry, and a second translates them into constraints that select candidate MOFs, each made of a metal node, organic linker, and matching topology. Each hypothesis is tested through four diagnostic beams that apply different subsets of its constraints, so comparing them shows whether geometry, chemistry, or metal choice drives performance. Even when blind to the global property landscape of databases, LLM4MOF concentrates its search on top-performing structures across six adsorption, separation, and electronic-structure tasks within 400 property evaluations. The same loop also generates new MOFs de novo and validates them in live simulation, where it adapts the geometry to each requested condition, outperforming random search and a genetic algorithm at roughly $1 per campaign. LLM4MOF shows that language-model agents can run interpretable, simulation-grounded inverse design without training a model per objective.

---

## 论文详细总结（自动生成）

# 论文详细总结：《基于大型语言模型代理的可解释金属有机框架逆向设计》

## 1. 核心问题与整体含义

- **研究动机**：金属有机框架（MOFs）的逆向设计面临组合空间庞大、属性标签昂贵（需仿真或实验）、且大多数机器学习模型无法解释“结构为何成功”的困境。传统遗传算法和生成模型依赖大量标注数据，且难以提供化学设计洞察。
- **整体意义**：提出 LLM4MOF——一个闭环多代理框架，利用大型语言模型（LLM）代理进行可解释的化学推理，自动构建并评估候选MOFs，无需为每个目标训练专用模型，从而以极低成本实现模拟驱动的逆向设计。

## 2. 方法论

### 核心思想
- 将化学推理与结构构建分离：LLM代理负责提出可解释的设计假设（如金属节点、连接体、孔隙几何、功能化学），再由自动化模块转化为约束、匹配候选、仿真评估，最终将结果作为反馈迭代优化。
- 通过诊断束（Diagnostic Beams）隔离不同设计轴（几何、化学、金属选择），实现属性归因。

### 关键技术细节
1. **假设生成器（Agent 1）**：基于用户自然语言目标和历史反馈，生成结构化的JSON假设，包含机制、节点/连接体特征、几何窗口等。不指定具体拓扑或材料名称，仅使用二连接体。
2. **约束翻译器（Agent 2）**：将Agent 1的假设转换为可搜索的约束JSON（金属、连接体、全局要求、几何过滤器），使用受控词汇，不引入新假设。
3. **匹配器（Matchmaker）**：根据约束选择候选MOFs，并组织成四个诊断束：
   - Beam 1：全假设（所有约束）
   - Beam 2：化学（仅金属-连接体，去除几何）
   - Beam 3：金属（仅金属，无连接体/几何限制）
   - Beam 4：随机基线（无约束）
4. **假设测试**：两种模式：
   - 数据库模式：从预计算属性表检索候选MOF属性（不进行新仿真）
   - 发现模式：从头组装结构（PORMAKE）、力场优化（LAMMPS/UFF）、几何表征（Zeo++）、吸附仿真（RASPA3 GCMC）
5. **反馈生成器**：生成最新的证据报告，并压缩历史反馈为固定大小的记忆账本（Memory Ledger），控制提示长度。
6. **闭环迭代**：每个任务运行10次自主迭代，每次最多评估40个候选（每束最多10个），总计400个。

### 算法流程（文字描述）
用户输入目标（如“设计最大化77K、5bar下体积H₂吸附量的MOF”），Agent 1给出初始假设→Agent 2转为约束→匹配器从数据库/构建模块中选出四束候选→评估属性→反馈生成器比较束间性能，返回给Agent 1→Agent 1修正假设，重复直至10次迭代。

## 3. 实验设计

### 数据集/场景
- **数据库模式**：三个数据库：
  - PORMAKE衍生数据库：14,173个结构（H₂吸附任务）
  - hMOF数据库：51,145个结构（CH₄、CO₂吸附、Xe/Kr选择性）
  - QMOF数据库：20,373个结构（带隙最大化和最小化）
- **发现模式**：从头生成MOFs（构建模块来自PORMAKE固定库），实时仿真评估。任务：H₂吸附（77K, 5bar）和（160K, 5bar）。

### Benchmark
- 数据库模式：与各数据库的全部分布、数据库中位数、top 10%、top 1%比较。
- 发现模式：与随机搜索（Beam 4）和遗传算法（GA）对比，基于相同评估预算（每迭代40个结构，共10次迭代，5次重复）。

### 对比方法
- 随机搜索（Beam 4）
- 代理辅助遗传算法（GA）：初始40个随机结构，每迭代40个新结构，使用累积历史。

## 4. 资源与算力

- **LLM推理**：使用GPT-5.2（未给出具体版本细节）。每个10次迭代的活动平均使用约**0.57百万tokens**，LLM推理成本约**1.06美元**。
- **仿真**：每个活动约执行**380次RASPA3 GCMC模拟**。
- **未明确给出**：GPU型号、数量、训练时长（因为LLM仅用于推理，无需训练新模型；MOF组装和仿真在CPU上进行，文中未详述硬件规格）。
- 注：发现模式中使用了MOF2Zeo几何预排名器，但该预排名器训练细节见补充材料，未在主文中明确其训练算力。

## 5. 实验数量与充分性

- **实验数量**：
  - 数据库模式：6个任务（H₂、CH₄、CO₂、Xe/Kr、带隙最大化、带隙最小化），每个任务5次独立重复，10次迭代。
  - 发现模式：2个任务（77K H₂、160K H₂），各5次重复。
  - 消融实验：包括记忆账本消融（补充材料图S10），以及H₂吸附条件变化（体积vs质量、5bar vs 100bar）的对比（补充图S3/S4）。
- **充分性评价**：
  - **充分**：覆盖多种吸附/分离/电子性质任务，数据库与发现模式互补，重复次数合理（5次），基线对比（随机、GA）公平（同等预算）。
  - **客观**：四个诊断束设计允许隔离不同设计轴的影响，反馈生成器避免记忆账本混入随机样本。
  - **风险**：发现模式仅验证了H₂吸附，未拓展到其他性质（如带隙、选择性）的实时仿真，可能限制泛化性。

## 6. 主要结论与发现

- LLM4MOF在**400次属性评估内**（数据库模式）即可将搜索集中到各数据库的top 1%或top 10%区域。
- 诊断束分离揭示了**不同任务的关键设计轴不同**：吸附/分离任务中几何约束最关键；带隙最大化中化学起主导；带隙最小化无单一轴起决定作用。
- 在发现模式下，性能**优于随机搜索和遗传算法**（77K H₂：Beam 1中位数29.3 g/L vs GA 21.8 g/L vs 随机14.3 g/L）。
- 闭环能**自我修正假设**（如CO₂任务：初始强调开放金属位，后根据反馈转向刚性芳香壁与微孔限制）。
- **设计规则可迁移**：发现模式独立于数据库，也收敛于与数据库模式一致的“紧凑微孔”设计原则（针对体积H₂吸附）。
- **低成本**：每活动LLM推理成本仅约1美元。

## 7. 优点

- **可解释性**：通过四种诊断束对假设进行归因，每个迭代的输出都能指出性能提升来自几何、化学还是金属选择，而非黑箱模型。
- **零任务专属训练**：无需为每个目标训练新的ML模型，仅需LLM推理和通用仿真模块。
- **低数据需求**：无需预标注大型数据集，仅需少量评估即可聚焦高性能区域。
- **自适应修正**：Agent 1能根据反馈调整假设（如纠正错误的化学直觉）。
- **轻量级成本**：LLM推理费用极低，仿真次数可控（约380次/活动）。
- **通用性**：框架可扩展至其他性质（如DFT计算）及其他吸附/分离目标，只需更换最终仿真模块。

## 8. 不足与局限

- **构建模块限制**：候选MOF仅限于PORMAKE固定库中的二连接体链接器、单一金属节点和给定拓扑，未涵盖更复杂的多连接体或非传统拓扑。
- **发现模式验证不足**：仅对H₂吸附进行了实时仿真验证，未扩展到其他任务（如CH₄吸附、带隙）的发现模式，缺乏通用性证据。
- **单目标设计**：当前每次循环只能处理单一目标，无法同时优化多目标（如选择性+容量）。
- **依赖LLM预训练知识**：Agent 1的化学假设质量依赖于GPT-5.2的训练语料，可能对新颖化学或稀有系统存在偏差或盲区。
- **记忆账本压缩**：为控制提示长度压缩历史反馈，可能丢失早期重要信息；消融实验虽支持设计，但未与无压缩基线对比（仅比较有无记忆账本，参考补充图S10）。
- **未涉及实验验证**：所有结构为计算生成，未与真实合成或表征结果对照，存在力场近似误差（如UFF可能不准）。
- **计算成本未完全透明**：虽提到LLM成本低，但未量化LAMMPS/RASPA模拟的CPU时间或GPU需求，难以复现总成本。

（完）
