---
title: Labeling Training Data for Entity Matching Using Large Language Models
title_zh: 使用大语言模型标注实体匹配的训练数据
authors: "Aaron Steiner, Christian Bizer"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28823v1"
tags: ["query:llm-dm"]
score: 9.0
evidence: 大语言模型作为教师为实体匹配标注训练数据
tldr: 实体匹配中，大型语言模型直接使用时推理慢且成本高，而传统方法需要任务特定的训练数据。本文提出知识蒸馏流程，用LLM作为教师标注训练对，训练更小的学生模型Ditto。在五个基准测试上，学生模型的性能与使用基准训练集训练的模型相差不到2个F1点。标注成本仅28-41美元，而人工标注需470小时。推理速度比直接使用LLM快41.5至534倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 849, \"height\": 1288, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1821, \"height\": 913, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 881, \"height\": 502, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28823v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1498, \"height\": 1060, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 853, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 826, \"height\": 473, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1408, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1231, \"height\": 1316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 867, \"height\": 276, \"label\": \"Table\"}]"
motivation: 避免为实体匹配手动标注大量任务特定训练数据，利用LLM的强性能但降低其高推理成本和延迟。
method: 使用LLM作为教师模型，结合对选择策略标注训练对，然后通过知识蒸馏训练学生模型（如RoBERTa或Ditto）。
result: 学生模型在Abt-Buy等五个基准上F1分数与使用人工标注训练集训练的模型相近（差距<2点），标注成本低至28-41美元，推理速度提升数十倍。
conclusion: 当前LLM配合合适对选择策略能大幅减少甚至消除实体匹配中的手动标注工作，是一种高效且经济的解决方案。
---

## 摘要
近期的大语言模型（LLMs）在无需特定任务训练数据的情况下，在实体匹配任务上取得了强劲性能。然而，将这些模型应用于大型候选对集合仍然缓慢且成本高昂。相比之下，使用传统机器学习方法或小型语言模型（SLMs，如RoBERTa）的实体匹配器推理速度更快，但需要特定任务的训练数据。本文研究是否可以通过知识蒸馏工作流来避免提供特定任务训练数据的需要，其中大语言模型作为教师模型来标注训练对，这些训练对随后用于训练更小的学生模型。我们从以下维度研究实体匹配的知识蒸馏：对选择策略、教师模型、标签后处理方法以及学生模型。我们使用Abt-Buy、Walmart-Amazon、WDC Products、DBLP-ACM和DBLP-Scholar基准评估了这些工作流，并将使用机器标注数据训练的学生模型性能与使用基准训练集训练的相同模型性能进行了比较。实验表明，使用机器标注集训练的学生模型与使用基准训练集训练的模型性能大致相当，剩余差异在两个方向上都低于2个F1点。使用GPT-5.2为所有五个基准标注训练集的成本为28.31至40.88美元，而手动标注相同训练集估计需要470小时的工作。在推理时，Ditto比直接使用大语言模型执行匹配任务快41.5至534倍。这些结果表明，当前的大语言模型结合合适的对选择方法，可以大幅减少甚至消除为实体匹配标注用例特定训练数据所需的人工努力。

## Abstract
Recent large language models (LLMs) achieve strong performance on entity matching without requiring task-specific training data. However, applying these models to large sets of candidate pairs remains slow and costly. In contrast, entity matchers using traditional machine learning methods or small language models (SLMs), such as RoBERTa, offer much faster inference but require task-specific training data.   This paper investigates whether the need to provide task-specific training data can be avoided by using knowledge-distillation workflows, in which an LLM serves as a teacher model to label training pairs that are subsequently used to train a smaller student model. We investigate knowledge distillation for entity matching along the following dimensions: pair-selection strategy, teacher model, label post-processing method, and student model. We evaluate the workflows using the Abt-Buy, Walmart-Amazon, WDC Products, DBLP-ACM, and DBLP-Scholar benchmarks, and compare the performance of student models trained with machine-labeled data to the performance of the same models trained using the benchmark training sets.   Our experiments show that student models trained using the machine-labeled sets perform approximately on par with models trained on the benchmark training sets, with the remaining differences in both directions staying below two F1 points. Using GPT-5.2 to label the training sets for all five benchmarks costs US\$28.31 to US\$40.88, whereas manually labeling the same training sets is estimated to require 470 hours of work. At inference time, Ditto is 41.5 to 534 times faster than directly using an LLM to perform the matching tasks.   These results indicate that current LLMs, when combined with a suitable pair-selection method, can substantially reduce or even eliminate the manual effort required to label use case-specific training data for entity matching.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是对该论文的结构化、深入且客观的中文总结。

## 1. 核心问题与整体含义

- **研究动机**：实体匹配（EM）是数据集成中的关键步骤，旨在判断不同数据源中的记录是否指向同一现实实体。现代深度学习方法（如基于Transformer的匹配器）性能优异，但其高度依赖高质量、任务特定的监督训练数据。手动标注训练数据成本高昂且耗时，尤其需要处理“困难样本”（corner cases），限制了方法的可扩展性。
- **核心问题**：大型语言模型（LLM）在零样本实体匹配中表现强劲，但直接用于大规模候选对推理时，速度慢且成本高。如何兼顾高性能与低成本、高效率？
- **解决方案**：本文提出并系统评估了一种**知识蒸馏**工作流，利用LLM作为“教师模型”自动标注训练数据，然后训练一个更小、更快的“学生模型”（如Ditto/RoBERTa）用于大规模在线推理。旨在回答：机器标注数据能否达到与人工标注数据相当的质量（“适用性”，fitness for use）？成本与效率收益如何？

## 2. 方法论：核心思想与流程

- **核心思想**：通过教师-学生架构，将LLM的强泛化能力“蒸馏”到高效部署的小模型中。核心是平衡**标注质量**、**标注成本**和**推理效率**。
- **工作流流程**：
    1.  **候选池生成**：将基准训练集拆回两个源表，为每个左侧记录生成20个候选对（18个通过嵌入相似度搜索的最近邻，2个随机采样），以包含匹配和非匹配。
    2.  **对选择策略**：决定将哪些候选对送交LLM标注，这是控制成本和质量的核心。比较了三种策略：
        - **相似性搜索**：为每个左侧记录，优先标注其最相似的候选对，直到获得至少一个正例和四个负例。若发现密集正例区域，则标注所有20个候选。这是基线方法。
        - **主动学习（基于特征）**：使用由逻辑回归、随机森林等组成的委员会，通过特征（属性相似度、嵌入余弦相似度）编码，选择委员会分歧最大的候选对进行标注。
        - **主动学习（基于Ditto）**：与（基于特征）类似，但使用Ditto模型的集成作为委员会，选择分歧最大的候选对。这是本文最推荐的方法。
    3.  **LLM标签标注**：使用LLM教师模型（如GPT-5.2）通过特定提示（要求输出JSON格式的`match: true/false`）为每个选中的候选对标注标签。
    4.  **标签后处理**：可选步骤，旨在修正错误标签。比较了两种方法：
        - **重新标注**（谨慎审查）：使用要求更保守、检查矛盾的提示，让LLM再次判断。结果可用于覆盖原标签或将不一致的标签丢弃。
        - **基于闭包的过滤**：构建匹配对图，通过移除“桥接边”（bridge edges，即连接不同组的唯一路径）来过滤可能错误的连接，防止过连接。
    5.  **学生模型训练**：利用生成的机器标注训练集训练下游学生模型（如Ditto）。

## 3. 实验设计

- **数据集与基准**：采用了五个广泛使用的标准EM基准数据集：
    - **产品领域**：`Abt-Buy`（自由文本标题）、`Walmart-Amazon`（结构化产品）、`WDC Products`（困难版，80%为困难样本，需泛化到未见实体）。
    - **文献领域**：`DBLP-ACM`和`DBLP-Scholar`（作者、标题、会议等）。
- **对比方法**：
    - **教师模型**：`GPT-5.2`（主力模型）、`Qwen 3.6 Plus`、开源的`Kimi K2.6`。
    - **学生模型**：`Ditto`（基于RoBERTa）、`XGBoost`（基于特征分类器）、三个不同规模的`Qwen3`系列LLM（0.6B, 1.7B, 8B）。
    - **基线**：直接使用基准人工标注训练集训练的学生模型性能；直接使用LLM进行零样本匹配的性能。
- **评估指标**：主要使用**F1分数**作为“适用性”的核心指标，并通过与基准人工标注集训练的模型对比来衡量机器标注集的质量。同时分析了**标注成本**（美元）和**推理速度**。

## 4. 资源与算力

- **训练算力**：文中提到每个Ditto学生模型的训练运行使用**单个48GB GPU**。未明确指定具体型号（如NVIDIA A40），也未提及总训练时长（小时数）。主动学习循环（特别是基于Ditto的）需要在每轮循环中训练多个模型（集成），其计算量远大于相似性搜索，但文中未量化这部分消耗。
- **推理算力**：学生模型推理在**单块NVIDIA A40 GPU**上完成，批大小为64。LLM API调用使用了10个并行工作线程。

## 5. 实验数量与充分性

- **实验数量**：非常丰富，覆盖了多个维度：
    - **主实验**：在不同数据集上对比三种对选择策略（共5个基准x 3种策略 = 15组对照）。
    - **消融实验**：探讨标签预算对F1的影响（每个数据集的曲线图）；比较不同教师模型（3种教师x 5个基准）；比较不同标签后处理方法（多种变体x 5个基准）；比较不同学生模型（多个模型x 5个基准）。
    - **成本与效率分析**：量化了标注成本和推理加速比。
    - **训练集结构分析**：分析了机器标注集与人工标注集在正例率、困难样本比例等方面的差异。
    - **标签噪声审计**：人工审计了测试集和教师模型标签的错误率。
- **充分性与客观性**：
    - **充分性**：实验设计系统且全面，围绕四个核心维度展开，考虑了多种模型组合和设置，提供了足够的证据支持其结论。
    - **客观性**：评估流程公开（代码和数据集），对照严格，控制了变量。报告了多次运行的平均值和标准差，并声明在标准差范围内视为性能相当，这增加了结果的可靠性。结论谨慎，如实报告了局限性（如WDC上的泛化差距）。

## 6. 主要结论与发现

1.  **性能相当**：使用机器标注训练集训练的学生模型，其F1分数与使用基准人工标注集训练的模型性能**相当**，差异在**±2个F1点**之内。在三个产品基准上甚至略有超出。
2.  **主动学习策略更优**：**主动学习（基于Ditto）** 策略整体最佳，尤其是在困难基准（如Abt-Buy和WDC）上，它能更有效地选择信息量大的样本，更快达到性能平台期。
3.  **成本与效率优势巨大**：
    - **标注成本**：用GPT-5.2标注所有五个基准训练集仅需**28.31至40.88美元**，而等效的手动标注估计需要**470小时**。成本降低数个数量级。
    - **推理速度**：训练好的Ditto学生模型推理速度比直接使用LLM快**41.5至534倍**。
4.  **教师模型泛化性**：性能不依赖于单一封闭API模型。开源的`Kimi K2.6`及`Qwen 3.6 Plus`作为教师模型也能取得和GPT-5.2类似的效果。
5.  **学生模型泛化性**：蒸馏工作流对**Ditto**直至**Qwen3**系列模型均有效。机器标注集提升了小模型（如Qwen3-0.6B）的性能，使其接近甚至超过其零样本表现。
6.  **标签后处理效果有限**：后处理在某些产品基准上略有帮助，但效果不稳定，未能一致地提升性能。

## 7. 优点

- **方法创新与实用性**：系统地将知识蒸馏框架应用于实体匹配的**训练数据标注**环节，而不是直接用于模型压缩。这切中了现实世界部署中的核心痛点（标注成本），具有很强的实用价值。
- **实验设计全面**：不仅比较了最终性能，还深入剖析了训练集组成、标签预算影响、标签噪声等多个关键方面，使得结论更加坚实。这种“知其然并知其所以然”的分析是亮点。
- **量化成本与效率**：通过货币和时间两种维度量化了机器标注的巨大优势，提供了非常有说服力的证据。
- **跨模型与跨领域验证**：在多种教师模型、学生模型以及产品/文献两个不同领域的基准上进行了验证，证明了方法的泛化性。
- **公开透明**：承诺公开所有代码、数据和配置，便于复现和后续研究。

## 8. 不足与局限

1.  **数据泄露风险**：公开的基准测试集可能已被教师LLM（如GPT-5.2）在预训练阶段“看到过”，这可能会高估其直接匹配F1分数。虽然作者通过人工审计发现教师会“纠正”基准测试集的标注错误来部分缓解，但无法完全排除影响。
2.  **单标注者审计**：对标签噪声的分析仅由第一作者一人完成，可能存在主观偏差，审计的可靠性不如多人交叉验证。
3.  **提示词固定**：所有实验仅使用一种特定的提示词。已有研究（Peeters et al. 2025）表明提示词与数据集存在强交互作用，更换提示词可能会影响绝对F1分数，尽管本文的**相对比较**结果（不同策略间的差异）仍是有效的。
4.  **参数范围受限**：
    - **学生模型**未尝试更大的模型（如DeBERTa等），也未探索Ditto的超参数优化。
    - **主动学习的种子集和每轮样本数量**等超参数是固定的，可能有更优的设置。
    - **候选池生成**策略（如k=20）是固定的，未探讨其敏感性。
5.  **泛化到未知实体**：在`WDC Products`基准上，蒸馏模型与直接使用LLM的差距（15 F1）远大于其他基准，表明在处理“训练时未见过实体”的任务时，蒸馏方法仍有显著改进空间。限度的主要是**学生模型**的泛化能力，而非标注流程。
6.  **忽略主动学习计算成本**：成本和效率分析仅考虑了**LLM API调用的费用**，而未计入主动学习中训练委员会模型（特别是Ditto集成）所需的**本地GPU计算资源**、时间和能耗。在某些场景下，这部分“选择计算成本”可能不可忽视。

（完）
