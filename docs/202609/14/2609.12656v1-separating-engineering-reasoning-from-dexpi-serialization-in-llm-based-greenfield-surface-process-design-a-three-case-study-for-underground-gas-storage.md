---
title: "Separating Engineering Reasoning from DEXPI Serialization in LLM-Based Greenfield Surface-Process Design: A Three-Case Study for Underground Gas Storage"
title_zh: 在基于大语言模型的地下储气库绿地地面工艺设计中分离工程推理与DEXPI序列化：一项三案例研究
authors: "Qingchuan Zhu, Shuyue Tong, Pengju Ren"
date: 2026-09-11
pdf: "https://arxiv.org/pdf/2609.12656v1"
tags: ["query:chemical-llm"]
score: 8.0
evidence: 基于LLM的过程设计，分离工程推理与DEXPI序列化
tldr: 在地下储气库地面工艺设计中，让大模型直接生成 DEXPI 标准序列化会带来沉重的表示负担。本文把工程推理与 DEXPI 序列化解耦，改用轻量工程中间表示（IR）来承载设计意图。三个案例的对比显示，IR 提示仅需 617-699 token，而直接生成需约 121.8k-121.9k token，表示负担大幅下降；直接输出 3 例中 2 例通过 XSD 校验，IR 全部通过最小校验器。但仍有 1 例 IR 因冷却状态矛盾被否决，说明减轻表示负担并不能自动消除工程不一致。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.12656v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1654, \"height\": 658, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12656v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1651, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12656v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1355, \"height\": 412, \"label\": \"Table\"}]"
motivation: LLM 生成 DEXPI 标准序列化负担沉重，需检验解耦工程推理能否定位表示失效与工程失效。
method: 固定 qwen3.8-max-0902 模型，围绕单压注入、采出外输、双压注入三案例，分别比较直接 DEXPI 生成与轻量工程 IR 生成。
result: 直接输出 3 例中 2 例 XSD 有效，IR 全部结构有效；提示词由约 121.9k token 降至 617-699 token，但 1 例 IR 因冷却状态矛盾被拒。
conclusion: 延迟 DEXPI 序列化能显著降低表示负担并隔离序列化失败，但仅减轻负担不足以消除工程不一致。
---

## 摘要
大语言模型能够生成工程描述和结构化工艺表示，但标准级序列化可能显著增加生成负担。这项诊断性研究考察，在受约束的地下储气库绿地地面工艺设计中，将工程推理与流程工业数据交换（DEXPI）序列化分离是否会改变表示失败和工程失败的发生位置。我们在三个案例中比较直接DEXPI生成与轻量级工程中间表示（IR）生成：单压力注入、采出与外输，以及双压力注入。全部六个条件均使用同一个固定模型快照 qwen3.8-max-0902，每个条件完成一次托管生成。直接输出在3个案例中有2个通过XSD验证，而全部3个工程IR输出在最小验证器下均结构有效。直接提示输入包含约121.8k–121.9k个token，而工程IR提示为617–699个token。工程可行性并未一致地偏向IR：一个IR输出因存在明确的冷却状态矛盾而被拒绝。这些案例还揭示了两种不同的直接DEXPI失败模式：工程不一致性和标准级序列化失败。观察到的比较表明，在这些所评估的方法组合中，推迟DEXPI序列化显著降低了表示负担，并有助于隔离序列化失败，但仅降低表示负担并不能消除工程不一致性。

## Abstract
Large language models can produce engineering descriptions and structured process representations, but standards-level serialization can substantially increase the generation burden. This diagnostic study examines whether separating engineering reasoning from Data Exchange in the Process Industry (DEXPI) serialization changes where representation and engineering failures occur in constrained greenfield surface-process design for underground gas storage. We compare Direct DEXPI generation with generation of a lightweight Engineering Intermediate Representation (IR) on three cases: single-pressure injection, withdrawal and export, and dual-pressure injection. All six conditions use one fixed model snapshot, qwen3.8-max-0902, with one completed hosted generation per condition. Direct outputs are XSD-valid in 2 of 3 cases, while all 3 Engineering IR outputs are structurally valid under a minimal validator. Direct prompt inputs contain approximately 121.8k-121.9k tokens, compared with 617-699 tokens for the Engineering IR prompts. Engineering feasibility does not uniformly favor the IR: one IR output is rejected for an explicit cooling-state contradiction. The cases also reveal two distinct Direct DEXPI failure modes: engineering inconsistency and standards-level serialization failure. The observed comparison shows that, in these evaluated method bundles, deferring DEXPI serialization substantially reduces representation burden and helps isolate serialization failure, but reducing representation burden alone does not eliminate engineering inconsistencies.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究背景**：绿地工艺设计（greenfield process design）要求同时满足多种正确性——工艺操作序列的表示、物料流与状态关系的保持，以及明确边界条件的满足。当设计还须以标准级数据格式交付时，模型必须在同一次生成中同时解决工程推理与序列化问题。
- **核心痛点**：DEXPI（Data Exchange in the Process Industry）是流程工业的标准机器可读信息模型，若能由 LLM 直接生成有效 DEXPI，则可与 DEXPI 兼容工程工具交换、程序化校验、转换为其他工程表示。但标准级序列化会显著增加生成负担：模型需在同一次生成中处理工程语义、标准级信息模型与 XML 序列化三重任务。
- **研究问题**：将工程设计与 DEXPI 序列化联合完成 vs. 将 DEXPI 序列化推迟到后续步骤，两种方式下表示负担与可观察失败模式有何差异？
- **整体含义**：这是一项**诊断性研究**（diagnostic study），目的不是证明某种方法更优，而是观察两种表示策略如何暴露不同的工程失败与序列化失败模式，从而为 LLM 驱动的流程设计工作流设计提供依据。

## 2. 论文提出的方法论

- **核心思想**：将“工程推理”与“DEXPI 标准级序列化”解耦，用轻量工程中间表示（Engineering IR）承载设计意图，把 DEXPI 序列化推迟到后续独立步骤。
- **两种方法组合（method bundles）**：
  - **Direct DEXPI 条件**：要求模型返回一份完整的 DEXPI 2.0 Process XML 文档，并在上下文中附带冻结的官方 DEXPI 2.0.0 schema 与模型文件作为参考；输出契约严格限定为 XML。
  - **Engineering IR 条件**：要求模型返回一个轻量 JSON 对象，仅含五个顶层字段——`case_name`、`scope`、`material`、`nodes`、`streams`；每条 stream 记录流量、压力、温度。输出契约较小，序列化责任被推迟。
- **关键技术细节**：
  - 两种条件被明确定义为**方法组合**而非单一自变量对比（Direct 条件额外包含大规模 DEXPI 参考上下文与严格 XML 输出契约）。
  - 提示模板固定保留为 `runtime/prompts/formal_direct_dexpi_v0.1.txt` 与 `runtime/prompts/formal_engineering_ir_v0.1.txt`。
  - 模型需自行决定主要操作、操作顺序与满足边界条件所需的中间工艺状态，未预设特定工艺配置。
- **评估指标设计**（三套独立判据）：
  - **表示有效性（Representation Validity）**：Direct DEXPI 用冻结官方 XSD 校验（`validate_xsd.py`）；Engineering IR 用最小结构校验器（`run_ir_once.py`）。IR 通过仅表示结构有效，不代表物理或工程有效。
  - **工程可行性（Engineering Feasibility）**：三档标签 ACCEPT / WARN / REJECT，考察工艺拓扑、流量与状态连续性、操作/状态一致性、边界条件满足情况；不进行严格过程模拟，不含完整状态方程或流体物性计算。
  - **方法内结果（Within-Method Outcome）**：PASS / WARN / FAIL，综合方法特定输出目标完成情况与工程可接受性。

## 3. 实验设计

- **场景/数据集**：三个受约束的地下储气库（UGS）地面工艺设计案例，均以设计基础（capacity、pressure、temperature、boundary constraints）作为输入：

| 案例 | 进料条件 | 产能 | 出口要求 |
|---|---|---|---|
| Case 1：单压注入 | 8.0–10.0 MPa，约 2 °C | 700,000 标准 m³/day | 注入井 ≤35 MPa，温度 ≤60 °C |
| Case 2：采出与外输 | 12.0–14.0 MPa，约 25 °C | 450,000 标准 m³/day | 外输 6.5–7.5 MPa，温度 ≤45 °C |
| Case 3：双压注入 | 8.0–9.0 MPa，约 15 °C | 总计 900,000 标准 m³/day（A 组 300,000，B 组 600,000） | A 组 20 MPa，B 组 30 MPa，温度均 ≤55 °C |

- **Benchmark 性质**：本文没有使用公开数据集或标准 benchmark；三个案例是作者自行构造的受约束设计基础，属于诊断性案例研究。
- **对比方法**：仅对比两种方法组合——Direct DEXPI 生成 vs. Engineering IR 生成；每个案例-方法条件各完成一次托管生成，共 6 次生成。
- **固定模型配置**：qwen3.8-max-0902，reasoning effort: none，temperature 0.7，top_p 0.8，top_k 20，repetition penalty 1.0，seed 1234，max completion tokens 16,384，n=1，preserve_thinking: false，stream: false。
- **推理环境**：通过阿里云 DashScope 的 OpenAI 兼容 API 进行托管推理；Python 3.10.11，OpenAI SDK 2.54.0。
- **证据保留**：所有 6 个条件的生成 XML/IR JSON、运行元数据与验证记录均保留在可复现包中，原始输出不做归一化或改写；提供 SHA-256 清单。

## 4. 资源与算力

- **论文未明确说明 GPU 型号、数量或训练时长**。研究使用的是托管推理（hosted inference），而非本地训练或自建算力。
- 文中仅提及推理通过阿里云 DashScope 的 OpenAI 兼容 API 完成，使用 Python 3.10.11 与 OpenAI SDK 2.54.0。
- 从 token 与延迟数据可间接反映资源消耗量级（见下表），但论文未给出硬件层面的算力统计：

| 案例 | 方法 | Prompt tokens | Completion tokens | Latency (s) |
|---|---|---|---|---|
| Case 1 | Direct DEXPI | 121,839 | 10,573 | 168.13 |
| Case 1 | Engineering IR | 617 | 590 | 11.45 |
| Case 2 | Direct DEXPI | 121,881 | 6,655 | 87.71 |
| Case 2 | Engineering IR | 659 | 434 | 10.03 |
| Case 3 | Direct DEXPI | 121,921 | 9,467 | 146.78 |
| Case 3 | Engineering IR | 699 | 1,078 | 21.09 |

- 作者明确说明延迟仅作描述性报告，不构成严格的模型性能结论，因为服务器负载、缓存与后端调度都会影响延迟。

## 5. 实验数量与充分性

- **实验规模**：共 6 次生成（3 个案例 × 2 种方法），每个条件仅完成一次托管生成，无结果依赖的重试、修复或重新生成。
- **消融实验**：无消融实验。两种条件被设计为方法组合对比，而非单一变量隔离（Direct 条件额外包含 DEXPI 参考上下文与严格 XML 输出契约，因此无法将差异归因于 XML vs. JSON 本身）。
- **充分性评估**：
  - 作为**诊断性案例研究**，6 个输出足以展示两类失败模式的存在与差异，但样本量极小，不支持统计推断或总体层面结论。
  - 作者本人在 Limitations 中明确承认：这是单一模型快照、三个测试案例、每个条件一次生成的诊断比较，结果仅针对这些特定运行，不能外推到其他模型或工艺系统。
  - **客观性**：原始输出被冻结保留、不修复不归一化，验证记录与 SHA-256 清单公开，具备较强的可审计性；工程可行性标签为事后定性评审，非预先设定的统计终点。
  - **公平性**：两种条件使用同一模型快照、同一组案例、同一套固定生成参数，但方法组合之间存在系统性差异（上下文规模、输出契约、验证器、序列化责任），因此并非严格受控对比。

## 6. 论文的主要结论与发现

- **表示有效性**：Direct DEXPI 在 3 个案例中 2 个通过 XSD 校验；Engineering IR 在 3 个案例中全部通过最小结构校验器。
- **工程可行性并不一致偏向 IR**：
  - Case 1：Direct 工程 REJECT（进料连接自环、主流股无流量值、后冷器温度从 48 °C 升至 55 °C 与冷却功能矛盾）；IR 为 WARN（存在 35.0→34.8→35.0 MPa 的小幅未解释压力序列）。
  - Case 2：Direct 为 WARN（流量计 FT-01 未连接主流路、压降后温度从约 25 °C 升至 48 °C 属合理性存疑）；IR 为 REJECT（明确命名为冷却的操作反而使温度从 12 °C 升至 35 °C，构成直接的操作/状态矛盾）。
  - Case 3：Direct 为 WARN 但 XSD FAIL（AggregatedDataValue 内出现意外 Data 元素）；IR 为 ACCEPT（15 节点 14 流股，双压分支展开为独立压缩级，末端 20 MPa/40 °C 与 30 MPa/40 °C，无操作/状态矛盾）。
- **两类 Direct DEXPI 失败模式被识别**：工程不一致性（Case 1）与标准级序列化失败（Case 3）；两者相互独立——通过结构门不保证工程一致，工程连贯也不保证标准级有效。
- **表示负担差异显著**：Direct 提示约 121.8k–121.9k tokens，IR 提示仅 617–699 tokens，Direct/IR 提示 token 比约 197×、185×、174×（Case 1–3）。
- **IR 的核心优势是“可检查性”而非工程正确性**：紧凑的节点-流股表示使 Case 2 的冷却状态矛盾容易被定位，但并未消除工程不一致。
- **总体结论**：在这些所评估的方法组合中，推迟 DEXPI 序列化可显著降低表示负担并有助于隔离序列化失败，但**仅降低表示负担本身不足以消除工程不一致**。

## 7. 优点

- **问题定位清晰且务实**：直面 LLM 生成标准级工程格式时的“表示负担”与“失败定位”问题，研究动机明确。
- **诊断性设计而非性能竞赛**：刻意将表示有效性、工程可行性、方法内结果三套判据分离，能够区分“通过结构门”与“工程正确”两类不同性质的失败。
- **方法组合的诚实表述**：作者明确承认 Direct 与 IR 是方法组合而非单一变量对比，避免了过度归因。
- **证据保留严谨**：6 个原始输出不做修复、归一化或结果依赖重试，附带 SHA-256 清单、验证记录与提示模板，可复现包公开，可审计性强。
- **失败模式的发现具有实践价值**：识别出“工程不一致”与“标准级序列化失败”两类独立失败模式，对后续工作流设计（如引入确定性 IR→DEXPI 转换器）有直接指导意义。
- **对局限性的坦率说明**：Limitations 部分对样本量、非受控变量、事后定性评审、无 EOS 计算等限制交代充分。

## 8. 不足与局限

- **样本量极小**：仅 3 个案例 × 2 种方法 = 6 次生成，每条件仅 1 次完成生成，不支持统计推断或总体层面结论。
- **单一模型快照**：仅使用 qwen3.8-max-0902，结果无法外推到其他模型或模型版本。
- **非受控变量对比**：Direct 与 IR 在提示上下文、输出契约、验证器、序列化责任上同时不同，尤其 Direct 条件携带大规模冻结 DEXPI 参考上下文，因此观察到的差异不能归因于 XML vs. JSON 本身。
- **工程评审非严格模拟**：工程可行性为事后定性评估，仅检查拓扑、流量/状态连续性、操作/状态一致性与边界条件，不含完整状态方程、流体物性计算或详细设备设计；Case 2 中 25 °C→48 °C 的温升仅作为合理性存疑处理，而非无条件数学矛盾。
- **验证器强度有限**：IR 仅通过最小结构校验器，不代表物理或工程有效性；Direct XSD 有效也不等于完整 DEXPI 语义一致性。
- **缺少下游转换验证**：本预印本未评估任何确定性的 IR→DEXPI 转换器，因此“推迟序列化”的完整工作流闭环未被验证。
- **延迟数据仅供参考**：受服务器负载、缓存与后端调度影响，不构成严格性能结论。
- **应用范围受限**：案例刻意限定于地下储气库地面工艺的三个特定场景，结论向其他流程工业场景的推广需谨慎。

（完）
