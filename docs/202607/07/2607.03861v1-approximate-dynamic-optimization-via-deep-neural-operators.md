---
title: Approximate Dynamic Optimization via Deep Neural Operators
title_zh: 基于深度神经算子的近似动态优化
authors: "Amin Nassaji, Ilias Mitrai, Prodromos Daoutidis"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03861v1"
tags: ["query:chemical-llm"]
score: 9.0
evidence: 将深度神经算子应用于化工过程（如批次聚合）的非线性动态优化
tldr: 化工过程中的非线性动态优化问题（如批处理温度曲线优化）计算成本高。本文提出深度神经算子作为代理模型，采用DeepONet和Fourier-enhanced DeepONet近似函数映射。在批量聚合反应器案例中，Fourier增强版预测误差显著低于标准DeepONet和前馈网络。该方法为动态优化提供高效精确的近似解，降低计算负担。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03861v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1092, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03861v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 434, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03861v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 1576, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03861v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 526, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03861v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 633, \"height\": 229, \"label\": \"Table\"}]"
motivation: 非线性动态优化问题的传统求解方法计算成本高，需要高效的近似替代模型。
method: 采用深度神经算子（DeepONet和Fourier-enhanced DeepONet）学习动态优化问题的函数到函数映射。
result: 在批量聚合反应器案例中，Fourier-enhanced DeepONet的预测误差低于标准DeepONet和前馈神经网络。
conclusion: 深度神经算子能有效近似动态优化解，其中Fourier增强架构表现更优，有望推广至其他过程优化问题。
---

## 摘要
本文解决了非线性动态优化问题，该类问题通过计算最优操作输入曲线来实现期望的输出曲线。此类轨迹优化问题常见于化工过程应用中，例如间歇过程，其中通过计算最优温度或进料曲线（对于流加过程）来强制执行随时间变化的产品质量曲线，从而严格控制反应速率或产热速率。我们提出了深度神经算子，用于近似函数到函数的映射，作为此类动态优化问题的替代解决方案。我们特别在间歇聚合反应器案例中使用了深度算子网络（DeepONets）和傅里叶增强深度算子网络，其中通过最优温度程序强制执行数均分子量和重均分子量曲线以及最终转化率目标。我们的结果表明，与标准DeepONet架构和标准前馈神经网络相比，傅里叶增强DeepONet架构在近似不同实例的动态优化问题解方面表现良好，实现了更低的预测误差。

## Abstract
This paper addresses the solution of nonlinear dynamic optimization problems that compute optimal manipulated input profiles to enforce desired output profiles. Such trajectory optimization problems commonly arise in chemical process applications, for example, batch processes where optimal temperature or feeding profiles (in case of fed-batch processes) are calculated to enforce time-varying product quality profiles, tightly controlling the reaction rate or rate of heat generation. We propose deep neural operators that approximate function to function mappings as surrogates for the solution of such dynamic optimization problems. We specifically employ deep operator networks (DeepONets) and Fourier-enhanced DeepONets in a batch polymerization reactor case study for which number-average and weight-average molecular weight profiles, together with a final conversion target, are enforced through an optimal temperature program. Our results show that the Fourier-enhanced DeepONet architecture performs very well in approximating the solution of the dynamic optimization problem for different instances, achieving a lower prediction error compared to the standard DeepONet architecture and standard feedforward neural networks.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：非线性动态优化问题在化工过程中广泛存在，例如间歇聚合反应器中通过最优温度曲线控制产品质量。这类轨迹优化问题通常需要针对不同的目标曲线、初始条件或时间范围反复求解，导致在线决策或大规模设计中的计算瓶颈。传统方法需要从零开始求解每个实例，计算成本高。
- **整体含义**：本文旨在提出一种替代方案——使用深度神经算子（DeepONets）作为代理模型，直接从问题规格（如期望的输出曲线）映射到最优控制轨迹，从而避免重复求解完整优化问题，实现高效近似。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将动态优化问题的解视为一个算子G，它将输入函数（如期望的产品质量曲线p(t)）映射到输出函数（最优控制轨迹u*(t)）。使用深度算子网络（DeepONet）来近似这个函数到函数的映射。
- **关键技术细节**：
  - **DeepONet架构**：包含分支网络（branch net）和主干网络（trunk net）。分支网络编码输入函数在离散采样点的值，输出系数向量b；主干网络编码查询时间t，输出特征向量φ(t)；预测值为两者点积：$\hat{u}(t) = b(p)^\top \phi(t)$。
  - **傅里叶增强**：在主干网络输入中增加傅里叶特征（正弦/余弦函数）：$\phi(\tilde{t}) = [\tilde{t}, \sin(2\pi k \tilde{t}), \cos(2\pi k \tilde{t})]_{k=1}^K$，其中$\tilde{t}=t/T_f$是归一化时间。这帮助模型捕捉长时间范围内的渐变和振荡。
  - **问题转化**：将连续最优控制问题离散化为有限维NLP（用SQP求解），以生成训练数据：输入为期望的Mn(t)、Mw(t)曲线和最终转化率X(Tf)，输出为最优温度曲线T*(t)。
- **算法流程**（文字说明）：
  1. 生成训练数据：对每个实例，使用SQP求解器求解约束NLP，得到最优温度曲线。
  2. 构建DeepONet：分支网络输入采样点处的目标曲线值，主干网络输入时间点及傅里叶特征。
  3. 训练：最小化点对点的回归损失（预测温度与求解器温度之差）。
  4. 推理：输入新实例的规格，直接输出近似最优温度曲线。

## 3. 实验设计
- **数据集/场景**：批处理甲基丙烯酸甲酯（MMA）聚合反应器案例。每个实例包含两个期望的产品质量曲线（数均分子量Mn(t)和重均分子量Mw(t)）以及标量最终转化率X(Tf)。通过求解SQP得到最优温度曲线。数据在归一化时间τ∈[0,1]上均匀采样Nt个点。共生成N个实例，固定20%作为测试集。
- **Benchmark**：优化器解（SLSQP）作为真值。
- **对比方法**：
  - Plain DeepONet A（较小）
  - Plain DeepONet B（更宽，参数更多）
  - Fourier-enhanced DeepONet（傅里叶增强）
  - 全连接前馈神经网络（FNN baseline，非算子方法，将离散化输入直接映射到离散化输出）
- **实验设置**：固定测试集（20%）用于所有实验；训练数据量变化（80%、60%、40%）；随机截断时间范围（horizon truncation）测试泛化能力。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量或训练时长。仅提到使用Adam优化器（学习率10^{-3}）训练固定迭代次数，实现基于DeepXDE框架。因此无法给出具体的算力信息。

## 5. 实验数量与充分性
- **实验组数**：主要报告了三组实验：
  1. **全时域预测**：80%训练/20%测试，展示三个代表性测试案例的图像比较。
  2. **数据效率实验**：训练分数分别为80%、60%、40%时，对比各模型的MAE和MSE。每组有表格（表I）展示数值结果。
  3. **随机时域截断实验**：每个案例截断至随机比例ri∈[0.5,1.0]，结果见表II。
- **充分性评价**：
  - 实验覆盖了标准全时域预测、不同训练数据量（数据效率）和变时域泛化能力，设计较为全面。
  - 对比了不同规模的标准DeepONet、傅里叶DeepONet和FNN基线，公平性较好（参数规模相近）。
  - 但**未进行消融实验**（如单独移除傅里叶特征的影响、不同主干网络深度等），也未涉及不同优化问题（如不同动力学模型）或更大规模数据集。结论基于单一案例（批处理聚合反应器），泛化性有待验证。

## 6. 主要结论与发现
- **Fourier-enhanced DeepONet在所有实验中表现最好**：在全时域预测中，其预测温度曲线与优化器解高度吻合，尤其能捕捉中批谷值和后期斜坡等关键形状。MAE和MSE均低于其他模型。
- **数据效率更高**：当训练数据减少时（80%→60%→40%），傅里叶DeepONet的误差增长最慢，而FNN和Plain DeepONet退化更明显。
- **变时域泛化能力强**：在随机时域截断设置下，傅里叶DeepONet仍保持低误差，而FNN基线显著恶化，显示了坐标算子模型在处理可变时间范围时的优势。
- **单纯增加Plain DeepONet宽度（参数）并不必然改善预测**，而傅里叶特征能更有效地提升性能。

## 7. 优点
- **创新性**：首次将深度神经算子（DeepONet）直接用于学习动态优化问题的解算子（而非系统模拟器或反向映射），提供了高效近似的新范式。
- **性能优势**：傅里叶增强后的DeepONet在准确性和数据效率方面均优于标准DeepONet和传统FNN，且能自然处理变时间范围，有利于实际运动控制场景。
- **实验设计合理**：控制了参数规模进行公平比较；使用固定测试集确保结果可重复；包含多种训练数据量和变时域测试，验证了鲁棒性。
- **可迁移性**：方法可推广到其他需要轨迹优化的化工过程（如流加发酵、过程启动/停止等）。

## 8. 不足与局限
- **实验覆盖有限**：仅在单一案例（MMA聚合反应器）上验证，缺乏在其他动力学系统或不同高维问题上的测试。
- **未提及算力开销**：没有报告训练时间、GPU型号等，无法评估实际计算成本。
- **缺乏消融实验**：未系统分析傅里叶特征数量、主干网络深度等超参数的影响，也未对比其他算子学习方法（如FNO、DeepONet变体）。
- **偏差风险**：训练数据由特定SQP求解器生成，解的质量依赖于初始猜测和求解器参数，可能存在次优解；FNN基线采用固定网格输入，可能不利于变时域场景。
- **应用限制**：当前方法假设输入/输出均为时间函数，对更复杂的问题（如多变量控制、非时间依赖的输入）可能需要扩展；离线训练依赖大量优化求解数据，生成成本仍较高。

（完）
