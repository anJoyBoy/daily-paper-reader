---
title: "Demonstrating TOFFEE: A Learned System for Synthesizing Data Agent Trajectories at Scale"
title_zh: 演示TOFFEE：大规模合成数据代理轨迹的学习系统
authors: "Ziting Wang, Yin Li, Zuhao Yang, Xiuchang Li, Jiale Bai, Gao Cong"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06233v1"
tags: ["query:llm-dm"]
score: 9.0
evidence: 为基于大语言模型的数据挖掘任务合成高质量数据代理轨迹
tldr: LLM驱动的数据代理在数据驱动决策中日益重要，但在异构企业环境中泛化困难。提出TOFFEE系统，通过蒙特卡洛树搜索、自适应模型选择和跨任务前缀复用，自动合成高质量数据代理轨迹。实验表明，TOFFEE能有效生成可扩展的轨迹数据，覆盖复杂分析任务。该方案为数据代理的监督微调和上下文学习提供数据支持，提升在异构环境中的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06233v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 791, \"height\": 784, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 795, \"height\": 793, \"label\": \"Figure\"}]"
motivation: 现有数据代理在异构企业环境中泛化能力不足，亟需大量高质量轨迹数据以适配新数据环境与分析工作流。
method: 采用蒙特卡洛树搜索，融合自适应模型选择与跨任务前缀复用，自动化合成数据代理轨迹。
result: TOFFEE有效生成可扩展的高质量轨迹数据，覆盖多种复杂分析任务并适应异构环境。
conclusion: 为数据代理在异构环境中的泛化提供可拓展的轨迹合成方案，支持下游微调与上下文学习。
---

## 摘要
基于LLM的数据代理在数据驱动决策中扮演着越来越重要的角色。然而，现有的数据代理难以泛化到未见过的数据环境和分析工作流，尤其是在异构企业环境中。这产生了对合成高质量数据代理轨迹的需求，这些轨迹能够捕获给定数据环境的复杂分析工作流。此类轨迹支持两个关键的下游用途：它们可以作为监督微调数据，使数据代理模型适应目标领域；也可以作为上下文学习演示，指导通用LLM在陌生数据环境中工作。因此，我们引入TOFFEE，一个通过蒙特卡洛树搜索（MCTS）结合自适应模型选择和跨任务前缀复用来从给定数据环境合成高质量数据代理轨迹的系统。我们展示TOFFEE能够有效地为异构环境中的复杂分析任务生成可扩展的轨迹数据。在本次演示中，我们将介绍TOFFEE的系统框架，包括其任务池构建、轨迹探索器和学习成本模型。我们还将介绍TOFFEE的web界面及其工作流，并演示两个端到端场景：用于数据代理微调的轨迹合成，以及演示增强的数据代理推理。

## Abstract
LLM-powered data agents are playing an increasingly important role in data-driven decision making. However, existing data agents struggle to generalize to unseen data environments and analytical workflows, especially in heterogeneous enterprise settings. This creates a growing need for synthesizing high-quality data agent trajectories that capture complex analytical workflows for given data environments. Such trajectories support two key downstream uses: they can serve as supervised finetuning (SFT) data that adapts data agent models to the target domain, and as in-context learning (ICL) demonstrations to guide general-purpose LLMs in unfamiliar data environments. Thus, we introduce TOFFEE, a system for synthesizing high-quality data agent trajectories from given data environments via Monte Carlo Tree Search (MCTS) with adaptive model selection and cross-task prefix reuse. We show that TOFFEE can effectively generate scalable trajectory data for complex analytical tasks across heterogeneous environments. In this demonstration, we present the system framework of TOFFEE, including its task pool construction, trajectory explorer, and learned cost model. We also introduce the web interface of TOFFEE and its workflow, and demonstrate two end-to-end scenarios: trajectory synthesis for data agent finetuning, and demonstration-augmented data agent reasoning.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：当前 LLM 驱动的数据代理（data agent）在异构企业环境中泛化能力不足，表现为在未见过的数据环境和分析工作流中频繁出现 SQL 错误、推理模糊、结论缺乏统计证据等。
- **需求**：需要一种自动化的方法为给定的数据环境合成高质量的多步轨迹数据（reasoning + tool calls + observations），用于监督微调（SFT）或上下文学习（ICL）演示，从而提升代理在陌生环境中的适应性。
- **整体目标**：设计一个能够大规模、低成本地合成可靠轨迹数据的系统，覆盖复杂分析任务（如多表连接、异常检测、统计计算等）。

## 2. 方法论：核心思想、关键技术细节与流程

- **核心思想**：结合蒙特卡洛树搜索（MCTS）、自适应模型选择（Learned Cost Model, LCM）和跨任务前缀复用，在给定数据环境下高效搜索高质量轨迹。
- **任务池构建（Task Pool Construction）**：
  - 连接数据环境（数据库、CSV、JSON等），通过轻量脚本探测数据依赖关系（如 `customer_id` 关联多个表）。
  - 对每个依赖，反向生成多步分析任务（如“计算各区域季度收入增长”），并执行验证确保任务可解且答案确定。
  - 拒绝 LLM 无需数据即可回答的琐碎问题，保证任务真实性和可验证性。
- **轨迹探索器（Trajectory Explorer）**：
  - 对每个任务进行 MCTS 搜索。节点是分析状态，边是工具调用（SQL、Python、文件读取等）。
  - 共享前缀（shared prefix）在同一数据环境的所有任务间复用（如 schema discovery 步骤只执行一次）。
  - 遇到执行错误时不丢弃部分轨迹，而是分支为修复路径，保留错误-诊断-修复序列作为训练数据。
  - 当最终输出与任务答案键匹配时，存储轨迹。
- **学习成本模型（Learned Cost Model, LCM）**：
  - 从当前状态提取特征向量（轨迹进度、schema 覆盖度、错误历史等）。
  - 对每个可选配置（模型、上下文长度、推理努力）预测回报 μ 和探索奖励 σ，选择最优配置和分支宽度 K。
  - 通过在线学习（ridge regression）持续更新参数，形成搜索与选择的闭环。
- **下游用途**：输出轨迹直接用于 SFT（如 TOFFEE-9B、TOFFEE-27B）或作为 ICL 演示（检索相似轨迹注入 prompt）。

## 3. 实验设计

- **数据集 / Benchmark**：
  - **KramaBench**：多步分析任务基准。
  - **DSBench**：数据科学任务基准。
- **对比方法**：
  - **Single-Pass**：单次生成轨迹，失败则丢弃。
  - **Best-of-N**（N=5）：独立尝试多次，保留最佳。
  - **TOFFEE w/o LCM**：使用均匀配置的 MCTS（无自适应模型选择）。
  - **TOFFEE**（完整系统）。
  - 下游对比：OpenAI o3、Qwen3.5-27B、Qwen3.5-27B + ICL、TOFFEE-27B（SFT 微调）。
- **评估指标**：
  - **轨迹质量**：0-1 分，基于步骤执行成功率、分析进展和答案与任务答案键的确定性匹配。
  - **下游得分**：在 KramaBench 和 DSBench 上的整体得分。
- **实验设置**：所有方法共享相同的 agent prompt 和工具访问权限，Best-of-N 固定 N=5。

## 4. 资源与算力

- **硬件平台**：双 AMD EPYC 9555 CPU，8 张 NVIDIA H100 GPU（Ubuntu 服务器）。
- **模型调用**：通过 OpenRouter 调用外部 LLM API（如 GPT-4、Qwen3.5 等）。
- **训练时长**：论文未明确记录 SFT 训练时长或整体轨迹合成时间。

## 5. 实验数量与充分性

- **实验组数**：主要展示了两组量化结果：
  - 图 3(a)：轨迹质量 vs 预算（4 条曲线，每个方法重复？未说明）。
  - 图 3(b)：下游得分（4 个柱状图）。
- **消融实验**：通过 TOFFEE w/o LCM 与 TOFFEE 对比，证明 LCM 的必要性；但缺乏对任务池构建策略、跨任务前缀复用、MCTS 迭代次数等组件的独立消融。
- **公平性**：对比方法使用相同 agent prompt 和工具集，预算一致；但未讨论随机波动或多次运行的平均/方差。
- **充分性评价**：作为演示短文，实验规模有限，但核心对比清晰，初步验证了系统有效性。更充分的实验（如更多 benchmark、不同预算段、多种模型底座）需要后续论文补充。

## 6. 主要结论与发现

- TOFFEE 在相同 per-task 预算下合成轨迹质量显著优于 Single-Pass 和 Best-of-N，且质量提升在预算较低时更明显。
- 自适应 LCM 是关键：TOFFEE 始终优于去掉 LCM 的版本（TOFFEE w/o LCM）。
- 使用 TOFFEE 轨迹进行 ICL 可提升 Qwen3.5-27B 的表现，而 SFT 微调（TOFFEE-27B）在该模型上超越 OpenAI o3 等前沿推理模型。
- 系统能够自动生成可执行、可验证的任务，并导出高质量轨迹用于下游微调或推理增强。

## 7. 优点

- **自动化程度高**：从任务生成到轨迹搜索全自动，无需人工标注，适合大规模企业环境。
- **预算感知与效率**：LCM 自适应选择模型配置，在简单步骤使用低成本模型，复杂步骤投入更多资源，整体预算利用率高。
- **错误利用**：不丢弃失败路径，将其转化为修复轨迹，丰富训练数据的多样性。
- **跨任务复用**：共享前缀减少冗余计算，尤其对大型数据环境效果显著。
- **演示界面友好**：提供实时 MCTS 树可视化，支持用户交互（配置模型池、查看中间状态），降低使用门槛。

## 8. 不足与局限

- **实验覆盖有限**：仅两个 benchmark，且任务类型偏向 SQL 和结构化数据（表格、CSV），未涉及自然语言、图像等非结构化环境。
- **任务池构建可能遗漏复杂依赖**：依赖预定义 SQL 模板探测依赖，对高度非规范或稀疏关系的数据环境可能生成质量低或不可解的任务。
- **LCM 仍需在线学习启动**：早期搜索阶段配置选择可能不稳定，依赖一定量的探索数据。
- **安全性考虑不足**：未讨论轨迹中可能包含的敏感数据泄露风险、错误答案的连锁效应，或对恶意任务的鲁棒性。
- **需依赖外部 LLM API**：合成过程持续调用 LLM，成本由 API 调用主导，自部署模型方案未验证。
- **作为演示论文，缺少更深入的消融和统计显著性分析**（如多轮随机种子、误差线等）。

（完）
