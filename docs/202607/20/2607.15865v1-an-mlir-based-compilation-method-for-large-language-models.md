---
title: An MLIR-Based Compilation Method for Large Language Models
title_zh: 基于MLIR的大语言模型编译方法
authors: "Pengchao Hu, Zhibin Xin, Yifan Chen, Yangyang Zhou, Liang Wang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15865v1"
tags: ["query:chemical-llm"]
score: 8.0
evidence: 大型语言模型的编译方法
tldr: 大型语言模型在AI加速器上部署面临模型导入和高效调度两大挑战。本文提出基于MLIR的编译方法，通过TopOp高级图方言表达模型语义，TpuOp硬件方言处理量化、层分组等芯片决策。将Transformer层拆分为prefill、prefill_kv、decode三阶段静态编译，适配并行提示处理和逐token生成。已在TPU-MLIR编译器和LLM-TPU项目中实现，支持Qwen、Llama等多系列模型及GPTQ、AWQ等多种量化部署形式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15865v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 664, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 1230, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1375, \"height\": 1003, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 502, \"height\": 927, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15865v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 616, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15865v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 757, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15865v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1373, \"height\": 284, \"label\": \"Table\"}]"
motivation: 现有LLM部署面临模型导入复杂和自回归推理调度低效两大问题，需要统一的编译框架来桥接框架与硬件。
method: 设计TopOp和TpuOp两层MLIR方言，将模型先表示为高层图方言，再逐层下降至硬件方言，并将Transformer层拆分为三阶段静态编译。
result: 在TPU-MLIR编译器上实现，支持Qwen、Llama、InternVL、MiniCPM-V等模型及GPTQ、AWQ等量化方式。
conclusion: 该方法有效解决了LLM导入和调度难题，实现了框架无关的编译流程，可扩展至多种生成模型和量化方案。
---

## 摘要
大语言模型已成为现代AI加速器上的主导工作负载，然而在专用硬件上部署它们仍面临两个核心挑战：如何将训练好的模型导入编译器友好的中间表示，以及如何在有限片上内存下高效调度自回归推理循环。本文提出一种基于MLIR（多层次中间表示）的大语言模型编译方法，通过两个算子方言TopOp和TpuOp进行说明。TopOp作为高层图方言，独立于源框架和目标芯片，负责表达模型语义；TpuOp作为目标硬件方言，承载芯片相关决策，如量化、层组和内存布局。模型首先表示为TopOp，然后逐层降级到TpuOp，最后生成可部署的二进制文件。此外，每个Transformer层被拆分为三个静态编译阶段：prefill（预填充）、prefill_kv（带历史键值缓存的预填充）和decode（解码），以适应提示并行处理和逐token生成的不同计算特性。该方法已在TPU-MLIR编译器{https://github.com/sophgo/tpu-mlir}和LLM-TPU部署项目\footnote{https://github.com/sophgo/LLM-TPU}中实现，支持包括Qwen、Llama、InternVL和MiniCPM-V系列在内的多种生成模型，以及GPTQ、AWQ和AutoRound等多种量化和部署形式。

## Abstract
Large Language Models (LLMs) have become the dominant workload on modern AI accelerators, yet deploying them on specialized hardware still faces two core challenges: how to import a trained model into a compiler-friendly intermediate representation, and how to efficiently schedule the autoregressive inference loop under limited on-chip memory. This paper presents an MLIR (Multi-Level Intermediate Representation) based compilation method for large language models, illustrated using two dialects of operators, TopOp and TpuOp. TopOp serves as a high-level graph dialect that is independent of both the source framework and the target chip, and is responsible for expressing model semantics; TpuOp serves as the target hardware dialect, carrying chip-related decisions such as quantization, layer groups, and memory layout. A model is first represented as TopOp, then lowered layer by layer to TpuOp, and finally a deployable binary is generated. In addition, each Transformer layer is split into three stages for static compilation: prefill, prefill_kv (prefill with historical key-value cache), and decode, so as to accommodate the different computational characteristics of prompt-parallel processing and per-token generation. The method has been implemented in the TPU-MLIR compiler{https://github.com/sophgo/tpu-mlir} and the LLM-TPU deployment project\footnote{https://github.com/sophgo/LLM-TPU}, supporting a variety of generative models including the Qwen, Llama, InternVL, and MiniCPM-V series, as well as multiple quantization and deployment forms such as GPTQ, AWQ, and AutoRound.

---

## 论文详细总结（自动生成）

# 论文总结：基于MLIR的大语言模型编译方法

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）在专用AI加速器（如TPU）上部署面临两大挑战：①如何将训练好的模型（来自PyTorch/HuggingFace）导入编译器友好的中间表示；②如何在有限片上内存下高效调度自回归推理循环（尤其是prefill和decode阶段具有不同的计算特性）。
- **背景**：传统手写算子库方法成本高、难以跟上模型演进；业界转向领域专用编译器。MLIR提供了可重用、可扩展的编译基础设施，允许通过自定义方言表达不同抽象层次的计算。
- **整体含义**：论文提出一种基于MLIR的完整编译方法，通过高层图方言（TopOp）和硬件目标方言（TpuOp）的两层设计，实现框架无关、芯片无关的模型导入，并针对自回归推理将Transformer层静态编译为三种阶段变体，从而实现高效部署。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 使用MLIR的两层方言：
  - **TopOp**：高层图方言，独立于源框架（PyTorch/TensorFlow）和目标芯片，表达模型语义（如MatMul、RMSNorm、RoPE、FAttention、MLP等）。
  - **TpuOp**：目标硬件方言，承载量化模式、数据类型、层组信息、内存地址、算子拆分等芯片相关属性。
- **编译流程**：模型检查点 → 分割为按层/按阶段的TopOp模块 → 逐层下降（lowering）为TpuOp → 独立编译每个模块 → 合并为部署二进制文件。
- **三阶段拆分**：每个Transformer层编译为三个静态形状变体：
  - **prefill**：处理所有输入token（并行），无历史KV cache，输出新KV cache。
  - **prefill_kv**：带历史KV cache的prefill，用于多轮对话或长上下文分段。
  - **decode**：每次处理一个token，依赖历史KV cache（不断增长）。
- **关键技术细节**：
  - 预计算cos/sin表（RoPE）并通过`top.Gather`索引，避免运行时计算。
  - 因果掩码采用固定大小的小mask（如128×128），在分块注意力中复用。
  - 编译时固定最大序列长度（如8K），支持长上下文分段调用多个prefill_kv模块。
  - 支持多种量化模式：对称/非对称INT8、BF16、F16、GPTQ、AWQ、AutoRound，以及直接通过编译（direct-through）已量化权重。
  - 模块化编译：按层和阶段分割，支持并行编译，运行时按需组合。

### 公式/算法流程（文字说明）
1. **模型导入**：使用`llm_convert.py`加载HuggingFace配置和权重，根据模型族分派转换器，生成TopOp MLIR（每个模块一个函数）。
2. **下降与编译**：每个TopOp模块经过lowering pass替换为TpuOp（附加量化参数、地址、层组等），再经过层组切片、内存分配、代码生成，输出目标二进制。
3. **运行时编排**（图5）：
   - 运行嵌入层；
   - 对每一层：若历史存在则调用prefill_kv，否则调用prefill，得到更新后的KV cache和隐藏状态；
   - 运行LM head得到logits，采样下一个token；
   - 在decode循环中，每次调用decode模块，重复直到EOS。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集/场景**：论文未明确列出具体标准数据集（如WMT、WikiText等）。仅提到支持多种模型系列，包括Qwen、Llama、InternVL、MiniCPM-V等。实验场景为实际部署的推理任务，涵盖纯文本和视觉语言模型。
- **Benchmark**：未提供标准benchmark对比（如延迟、吞吐量、显存占用等）。论文主要展示方法可行性，重点在编译流程的完整性和通用性。
- **对比方法**：无直接对比基线（如vLLM、TGI等）。仅在讨论中对比了动态KV cache管理（vLLM的paged attention），指出静态编译下必须固定地址布局。

## 4. 资源与算力

- **文中未明确说明**：没有给出具体GPU/TPU型号、数量、训练或编译时长等内容。仅提到方法在TPU-MLIR编译器和LLM-TPU项目中实现，目标硬件为“专用AI加速器”，但未披露具体规格。因此无法评估算力消耗。

## 5. 实验数量与充分性

- **实验数量**：论文没有设计传统对比实验或消融实验。主要贡献是提出编译方法和系统实现，并以代码仓库（tpu-mlir、LLM-TPU）作为验证。文中提到支持4种模型系列、多种量化形式，并列出典型性能优化（因果掩码、RoPE预计算、KV cache管理），但未给出量化对比或编译时间/推理速度数据。
- **充分性**：从学术实验角度，不足够充分。缺少定量指标（如延迟、吞吐量、精度损失）和与现有系统的对比。但作为方法论论文，重点在于设计原理和实现路径，因此可以接受。存在偏差风险：未说明是否在多种硬件上测试，优化效果是否通用。

## 6. 论文的主要结论与发现

- **主要结论**：基于MLIR的两层方言（TopOp/TpuOp）和三阶段静态编译方法能有效解决LLM导入和调度难题，实现框架无关的编译流程。模块化编译支持并行化和运行时灵活组合。该方法已在生产级编译器中实现，并支持多种主流LLM和量化方案。
- **关键发现**：
  - 将Transformer层拆分为prefill/prefill_kv/decode三阶段，使得每个阶段的张量形状固定，有利于静态编译和确定性内存分配，避免动态推理开销。
  - 预计算RoPE的cos/sin表并通过gather访问能消除运行时元素级计算，尤其在decode阶段受益明显。
  - 因果掩码使用固定小尺寸分块重用，可大幅减少带宽。
  - 模块化编译（每层独立编译）有利于并行和复用，支持长上下文分段处理。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **两层方言设计**：TopOp提供框架无关、芯片无关的高层语义，TpuOp承载芯片细节，层间下降路径清晰，可灵活适配不同硬件。
  - **三阶段拆分**：针对自回归推理的不同计算模式（并行prefill vs 串行decode）采用静态编译，平衡了动态性和编译确定性，是实际部署中的关键技巧。
  - **预计算RoPE**：将运行时计算转化为内存访问，适合计算受限的加速器。
  - **模块化编译与运行时组合**：支持并行编译、跨层复用、长上下文分段。
  - **多量化模式支持**：涵盖主流后量化方法（GPTQ、AWQ、AutoRound），体现了工程实用性。
- **实验设计上的亮点**：虽然缺乏定量对比，但公开了完整代码仓库和模型支持列表，便于复现和拓展。

## 8. 不足与局限

- **实验覆盖不足**：
  - 缺少标准化benchmark（如MLPerf、LongBench）上的延迟、吞吐量、能量效率对比。
  - 未进行消融实验：量化精度损失、三阶段拆分相对动态编译的提升幅度、模块化编译的加速比等均未量化。
  - 未提供编译时间、内存占用等工程指标。
- **偏差风险**：
  - 仅针对自家TPU（Sophgo TPU）进行实现，通用性未在其他硬件（如GPU、NPU）上验证。
  - 静态编译固定最大序列长度（如8K），处理超长上下文时需分段，可能引入额外延迟；与动态编译（如vLLM）的公平对比未知。
- **应用限制**：
  - 静态编译牺牲了灵活性，无法支持可变批大小、动态跳过层等优化。
  - KV cache地址在编译时固定，不支持paged attention，可能在大批量请求时造成内存浪费。
  - 未讨论训练/微调场景，仅聚焦推理部署。
- **论文形式**：偏向工程报告，缺乏理论分析（如编译复杂度、最优性保证）。部分内容（如性能优化）仅以示例形式给出，未深入论证。

（完）
