---
title: Labeling Training Data for Entity Matching Using Large Language Models
title_zh: 使用大型语言模型为实体匹配标注训练数据
authors: "Aaron Steiner, Christian Bizer"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28823v1"
tags: ["query:llm-dm"]
score: 8.0
evidence: LLM知识蒸馏用于实体匹配数据标注
tldr: 实体匹配中，大型语言模型性能强但推理慢且成本高。本文采用知识蒸馏流程，用LLM作为教师模型自动标注训练对，再训练小模型。在五个基准上，学生模型性能与人工标注训练集训练的模型相当（F1差距≤2点），标注成本仅$28-$41，而人工需470小时。推理速度比直接使用LLM快41-534倍。该方法可大幅减少实体匹配中的人工标注需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 849, \"height\": 1288}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 480}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1821, \"height\": 913}, {\"url\": \"assets/figures/arxiv/2606.28823v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 881, \"height\": 502}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28823v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 275}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 276}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 276}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1498, \"height\": 1060}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 853, \"height\": 330}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 826, \"height\": 473}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1408, \"height\": 277}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1231, \"height\": 1316}, {\"url\": \"assets/tables/arxiv/2606.28823v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 867, \"height\": 276}]"
motivation: 减少实体匹配中为小模型标注训练数据所需的人工努力。
method: 使用LLM作为教师模型，通过知识蒸馏流程自动标注候选对，训练RoBERTa等小模型。
result: 学生模型F1与人工标注训练集模型差距小于2点，成本低至$28-$41，推理速度提升41-534倍。
conclusion: LLM结合适当对选择方法可有效替代人工标注，降低实体匹配部署成本。
---

## 摘要
近期的大型语言模型（LLMs）在不需要任务特定训练数据的情况下，在实体匹配中表现出了强大的性能。然而，将这些模型应用于大量候选对仍然慢且昂贵。相比之下，使用传统机器学习方法或小型语言模型（SLMs，如RoBERTa）的实体匹配器推理速度快得多，但需要任务特定的训练数据。本文研究了是否可以通过使用知识蒸馏工作流来避免提供任务特定训练数据的需要，其中LLM作为教师模型来标注训练对，随后用于训练较小的学生模型。我们从对选择策略、教师模型、标签后处理方法和学生模型等维度研究实体匹配的知识蒸馏。我们使用Abt-Buy、Walmart-Amazon、WDC Products、DBLP-ACM和DBLP-Scholar基准评估了这些工作流，并将使用机器标注数据训练的学生模型的性能与使用基准训练集训练的相同模型的性能进行了比较。实验表明，使用机器标注集训练的学生模型的性能与在基准训练集上训练的模型大致相当，剩余差异在两个方向上都低于2个F1分数点。使用GPT-5.2为所有五个基准标注训练集的成本为28.31至40.88美元，而手动标注相同训练集估计需要470小时的工作。在推理时，Ditto比直接使用LLM执行匹配任务快41.5至534倍。这些结果表明，当前LLMs结合适当的对选择方法，可以大幅减少甚至消除为实体匹配标注用例特定训练数据所需的人工工作量。

## Abstract
Recent large language models (LLMs) achieve strong performance on entity matching without requiring task-specific training data. However, applying these models to large sets of candidate pairs remains slow and costly. In contrast, entity matchers using traditional machine learning methods or small language models (SLMs), such as RoBERTa, offer much faster inference but require task-specific training data.   This paper investigates whether the need to provide task-specific training data can be avoided by using knowledge-distillation workflows, in which an LLM serves as a teacher model to label training pairs that are subsequently used to train a smaller student model. We investigate knowledge distillation for entity matching along the following dimensions: pair-selection strategy, teacher model, label post-processing method, and student model. We evaluate the workflows using the Abt-Buy, Walmart-Amazon, WDC Products, DBLP-ACM, and DBLP-Scholar benchmarks, and compare the performance of student models trained with machine-labeled data to the performance of the same models trained using the benchmark training sets.   Our experiments show that student models trained using the machine-labeled sets perform approximately on par with models trained on the benchmark training sets, with the remaining differences in both directions staying below two F1 points. Using GPT-5.2 to label the training sets for all five benchmarks costs US\$28.31 to US\$40.88, whereas manually labeling the same training sets is estimated to require 470 hours of work. At inference time, Ditto is 41.5 to 534 times faster than directly using an LLM to perform the matching tasks.   These results indicate that current LLMs, when combined with a suitable pair-selection method, can substantially reduce or even eliminate the manual effort required to label use case-specific training data for entity matching.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

实体匹配（Entity Matching, EM）是数据集成中的核心步骤，需要判断两个记录是否指向同一真实世界实体。现代 EM 系统常使用基于 Transformer 的深度学习模型（如 Ditto、RoBERTa），这类模型性能好但依赖大量**任务特定的标注训练数据**。人工标注不仅昂贵，还需要覆盖边界案例（如同一产品族、兼容组件等），成本极高。大型语言模型（LLM）在零样本设置下能够直接进行实体匹配，性能强，但推理速度慢、成本高，不适用于大规模候选对场景。因此，论文探索了**知识蒸馏工作流**：使用 LLM 作为教师模型自动标注训练对，再利用这些标注数据训练小型学生模型（如 RoBERTa、小 LLM），以兼顾标注成本、推理速度和匹配性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 总体流程

给定两个源表（左表和右表），工作流包括：
1. **候选池生成**：将两个表中的记录用嵌入（`text-embedding-3-small`）计算余弦相似度，为每个左表记录生成 20 个候选对（18 个最近邻 + 2 个随机低相似度采样），确保包含匹配、难非匹配和易非匹配。
2. **对选择（Pair Selection）**：从候选池中选出需要教师模型标注的子集。比较三种策略：
   - **相似度搜索（Similarity Search）**：按相似度分批扫描，每个左表记录至少标注 7 个对（5 最相似 + 2 低相似度），若发现密集正类区则标注全部 20 个。
   - **主动学习（ML）**：基于特征（属性相似度 + 嵌入余弦相似度）的委员会（逻辑回归、随机森林等），按模型分歧度选择对。
   - **主动学习（Ditto）**：基于 Ditto 模型的委员会（bagged ensemble），按预测概率方差、熵等选择对。
3. **LLM 标注**：教师模型（如 GPT-5.2）使用统一提示（图 2）为每个选定对输出 `{"match": true/false}`。
4. **标签后处理（可选）**：
   - **重新标注（Relabeling）**：使用保守提示（图 3）再次审查，若决策改变则替换或丢弃。
   - **基于闭包的丢弃（Closure-based dropping）**：在正类图中移除桥接边（仅当组件中有 ≥ 3 个记录时），以消除可能错误的匹配连接。
5. **训练学生模型**：使用机器标注的训练集训练小型模型（如 Ditto、XGBoost、Qwen3 系列）。

### 2.2 关键技术细节

- 教师模型提示：基于 Peeters et al. (2025) 的“general-complex”任务描述，要求 JSON 格式输出。
- 候选池覆盖度：所有基准中覆盖了 99.56%–99.92% 的原始训练集正类对。
- 主动学习种子：初始 100 对（30% 正类），每轮选择最多 500 个委员会分歧最大的对。
- 后处理保留策略：`+Relabel`、`+Relabel drop`、`+Closure drop`、`+Cl.∧Rel. drop`（交集）、`+Cl.∨Rel. drop`（并集）。

## 3. 实验设计：数据集、基准、对比方法

### 3.1 数据集

使用五个标准 EM 基准：
| 基准名 | 领域 | 特点 |
|--------|------|------|
| Abt-Buy | 产品 | 长自由文本标题 |
| Walmart-Amazon | 产品 | 结构更规范 |
| WDC Products | 产品 | 80% 边界案例，测试集仅含训练集未见实体 |
| DBLP-ACM | 书目 | 作者、标题、年份、会议 |
| DBLP-Scholar | 书目 | 作者、标题、年份、会议 |

### 3.2 对比方法

- **教师模型**：GPT-5.2（默认）、Qwen 3.6 Plus、Kimi K2.6（开源权重）。
- **对选择策略**：相似度搜索、主动学习（ML）、主动学习（Ditto）。
- **后处理方法**：无、`+Relabel`、`+Relabel drop`、`+Closure drop`、`+Cl.∧Rel. drop`、`+Cl.∨Rel. drop`。
- **学生模型**：Ditto（RoBERTa-base）、XGBoost（基于相似度特征）、Qwen3-0.6B、Qwen3-1.7B、Qwen3-8B（LoRA 微调）。
- **基准**：每个学生模型在**原始人工标注的基准训练集**上训练的性能作为对照。
- **零样本基线**：Qwen3 系列直接零样本匹配；教师模型直接匹配测试集（表 6）。

### 3.3 评估指标

- F1 分数（多次运行均值 ± 标准差，种子 42,52,62）。
- 成本（美元，LLM API 调用消耗）。
- 推理时间（每千对和百万对）。
- 训练集组成（正类率、困难正/负对数等）。

## 4. 资源与算力

- **GPU**：每个训练运行使用**单块 48GB GPU**（具体型号未在正文明确，但第 4.8 节提到 Ditto 运行在 NVIDIA A40 上）。
- **训练硬件**：Ditto 训练批量大小 64，最大序列 256，学习率 5e-5，最多 50 轮早停。
- **训练时间**：论文未报告具体训练小时数，但推理时间表（表 7）给出了归一化数据。
- **LLM 调用费用**：GPT-5.2 标注全部五个数据集总计 **$28.31–$40.88**（不同选择策略）；手动标注估计 **470 小时**。
- **推理速度**：Ditto 比零样本 GPT-5.2 快 **41.5 倍**，比 Kimi K2.6 快 **534 倍**。

## 5. 实验数量与充分性

实验覆盖了以下维度，每项均重复 3 次取均值：
1. **对选择策略对比**（表 1）：3 种策略 × 5 个数据集 = 15 组。
2. **标签预算影响**（图 4）：3 种策略在不同规模（从 1k 到 20k 对）下的性能曲线。
3. **教师模型对比**（表 2）：3 种教师 × 5 个数据集 = 15 组。
4. **后处理方法对比**（表 3）：6 种后处理变体 × 5 个数据集 = 30 组。
5. **学生模型对比**（表 4）：4 种学生（XGBoost、Ditto、3 种 Qwen3） × 4 种训练源（基准 + 3 种机器标注） + 零样本 = 大量组合，共 5 个数据集。
6. **教师零样本直接匹配**（表 6）：3 种教师 × 5 个数据集。
7. **推理时间**（表 7）：7 种模型 × 1k/1M 对。
8. **训练集组成分析**（表 8）：4 种训练源 × 5 个数据集 × 多个统计量。
9. **标签错误审计**（表 9）：1,028 个测试对人工审查，比较教师与基准的错误率。
10. **成本计算**（表 5）：3 种策略 × 5 个数据集。

**充分性**：实验设计系统全面，控制变量法逐一比较各维度。多次重复（3 种子）确保了统计可靠性。公平性：学生模型在机器标注和人工标注上的训练参数、评估脚本完全一致。

**不足**：所有实验基于公开基准，可能存在于 LLM 预训练数据中（论文亦指出此局限）；审计仅由一位作者完成（虽然盲审且覆盖全部分歧对）。

## 6. 论文的主要结论与发现

1. **学生模型性能与人工标注相当**：机器标注集训练的学生模型在 F1 上与基准训练集训练的差距小于 **2 个点**（±1.78 F1）。以主动学习（Ditto）为最佳策略，在三个产品基准上甚至略超基准（+0.23 ~ +1.59 F1）。
2. **主动学习优于相似度搜索**：主动学习（Ditto）在 4/5 基准上最佳，尤其在需要更多正类的场景（Abt-Buy、WDC）提升明显；书目基准 F1 在 1,000 对后即达平台。
3. **教师模型选择影响有限**：三种教师模型（GPT-5.2、Qwen 3.6 Plus、Kimi K2.6）的差距小于 3 F1，开源权重 Kimi K2.6 与最好教师相近。
4. **后处理可辅助但非通用**：`+Relabel` 在三个产品基准上有 0.72–1.56 F1 的提升，但书目基准无效；闭包丢弃法普遍下降。
5. **成本效率极佳**：GPT-5.2 标注全部五个数据集仅需 **$28–$41**，而人工估计 **470 小时**（即使 $10/小时也远高于 LLM 成本）。推理时 Ditto 比直接 LLM 快 **41–534 倍**。
6. **训练集组成差异**：主动学习显著提高正类率（最高 +12.7 pp）并选择更多困难正类，而相似度搜索倾向于仿照基准平衡。
7. **教师标签质量接近人工**：盲审发现教师错误率（0.98%–4.49%）与基准测试集错误率（0.53%–2.52%）相近，Kimi K2.6 甚至略低于基准。

## 7. 优点

- **方法系统全面**：覆盖知识蒸馏的四个关键维度（对选择、教师、后处理、学生），并给出清晰对比。
- **成本量化扎实**：不仅报告 F1，还详细列出 API 费用、人工估计、推理时间，直观展示实用性。
- **开源权重教师验证**：证明了工作流不依赖封闭 API，Kimi K2.6 也可达到类似效果，提升了可复现性。
- **训练集结构分析**：深入分析正类率、困难对组成等，解释为什么机器标注集有效，而不止于最终 F1。
- **标签错误审计**：主动检查基准集和教师标签的错误率，增强结论可信度。
- **大规模实验覆盖**：5 个标准基准 × 多种配置，统计稳定（3 次重复）。

## 8. 不足与局限

- **基准可能存在数据污染**：测试集可能已被 LLM 预训练数据包含（论文第 4.11 节承认），可能高估零样本匹配性能。但标签审计发现教师模型与人工不一致，说明不是简单记忆。
- **单标注者审计**：训练和测试集的错误审计由第一作者一人完成，虽盲审且覆盖所有分歧对，但缺少交叉验证。
- **WDC 未见实体挑战**：在测试实体完全未见于训练集的情况下，学生模型（Ditto）的 F1 比直接 LLM 低 15 分，说明小模型泛化能力有限，机器标注无法弥补模型容量差距。
- **提示固定**：所有实验仅使用一种提示模板（图 2），提示设计可能会影响结果（如 Peeters et al. 2025 所指出）。
- **候选池依赖嵌入质量**：候选对生成使用 `text-embedding-3-small`，该嵌入的缺陷可能导致部分正类丢失（但论文表明覆盖度 > 99.5％）。
- **主动学习计算开销**：主动学习（Ditto）需要每轮训练多个 Ditto 模型，虽然标注 token 成本低，但训练计算成本高，未在成本表（表 5）中计入。
- **后处理方法效果不一致**：闭包丢弃法未在基准上带来提升甚至下降，缺乏通用性。
- **实验仅覆盖 2026 年前的模型**：教师模型包括 GPT-5.2（未来命名？）、Qwen 3.6 Plus 等，实际版本和 API 价格可能变化。

（完）
