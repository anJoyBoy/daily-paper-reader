---
title: Query-Centric Optimization of AI Workflows via Approximate Query Processing and Proxy Models
title_zh: 基于查询的AI工作流优化：通过近似查询处理和代理模型
authors: "Huayi Wang, Jun Xu, Gromit Yeuk-Yin Chan"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00254v1"
tags: ["query:chemical-llm"]
score: 8.0
evidence: 使用近似查询和代理模型优化LLM工作流
tldr: "现代AI工作流（如LLM后训练、推理任务）可视为代价高昂的声明式查询。本文提出两种数据库技术：近似查询处理（AQP）将工作流转化为在线聚合，动态采样并提前终止；代理模型（PM）训练轻量决策树预过滤高置信样本。在TPC-DS上，AQP减少85-90%模型调用且误差<10%，PM减少60-70%。在LLM流水线上，AQP减少20-50%调用且精度损失<5%，PM减少评分时间达19倍。表明经典数据库技术无需修改模型即可大幅降低开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00254v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 852, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 867, \"height\": 979, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1849, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1852, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1849, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1841, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1843, \"height\": 351, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00254v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1840, \"height\": 359, \"label\": \"Figure\"}]"
motivation: AI工作流需频繁调用大型模型或奖励函数，计算成本高，亟需减少昂贵评估次数。
method: 策略一为近似查询处理（AQP），在线聚合样本并自适应提前终止；策略二为代理模型（PM），训练轻量决策树预过滤易判样本。
result: "TPC-DS上AQP减少85-90%调用（误差<10%），PM减少60-70%；LLM流水线上AQP减少20-50%调用（精度损失<5%），PM加速19倍。"
conclusion: 经典数据库技术可有效优化AI工作流，在不改动模型或管道前提下大幅降低计算成本。
---

## 摘要
许多现代AI工作流，从大语言模型后训练流程到智能体推理任务，都可以表示为声明式查询，其昂贵的谓词由大型模型或奖励函数评估。我们提出这些工作流的查询中心化表述，并展示了经典数据库技术，即近似查询处理（AQP）和基于代理模型（PM）的过滤，可以在不改变底层模型或流程的情况下大幅减少昂贵模型调用的次数。我们的第一个策略将工作流视为在线聚合问题：它逐步采样记录，维护带有置信区间的运行聚合估计，并在区间稳定后提前终止，当估计值落在用户指定的误差范围内时接受该估计值。第二个策略在一小组由预言机标注的样本上训练一个轻量级、CPU驻留的决策树，并用它来预过滤那些结果可以高置信度预测的记录，仅将不确定的记录路由给昂贵模型。我们在TPC-DS聚合查询以及真实的LLM后训练流程（包括数学推理、通用指令遵循和代码生成）上评估了这两种策略。在TPC-DS上，策略AQP在保持聚合误差低于10%的同时，在平衡分布下于预言机调用量的10-15%达到自适应停止点，减少了85-90%；策略PM减少了60-70%的预言机调用。在LLM流程上，策略AQP在预言机调用量的20-50%达到自适应停止点，结构化数学和代码任务的准确率损失低于5%；由奖励模型评分的开放式指令遵循显示更大但有界的减少。策略PM在结构化任务上将奖励模型评分时间减少了高达19倍，准确率损失低于10%。

## Abstract
Many modern AI workflows, ranging from LLM post-training pipelines to agentic reasoning tasks, can be expressed as declarative queries whose expensive predicate is evaluated by a large model or reward function. We propose a query-centric formulation of these workflows and show that classical database techniques, namely approximate query processing (AQP) and proxy-model (PM) based filtering, can substantially reduce the number of expensive model invocations without requiring changes to the underlying models or pipelines. Our first strategy treats the workflow as an online aggregation problem: it progressively samples records, maintains a running aggregate estimate with a confidence interval, and terminates early once the interval stabilizes, accepting the estimate when it falls within a user-specified error bound. Our second strategy trains a lightweight, CPU-resident decision tree on a small set of oracle-labeled examples and uses it to pre-filter records whose outcome can be predicted with high confidence, routing only uncertain records to the expensive model. We evaluate both strategies on TPC-DS aggregate queries and on real LLM post-training pipelines including math reasoning, general instruction following, and code generation. On TPC-DS, Strategy AQP keeps aggregate error under 10% while reaching its adaptive stopping point at 10-15% of oracle calls under balanced distributions, an 85-90% reduction, and Strategy PM reduces oracle calls by 60-70%. On LLM pipelines, Strategy AQP reaches its adaptive stopping point at 20-50% of oracle calls with less than 5% accuracy loss on the structured math and code tasks; open-ended instruction following, scored by a reward model, shows a larger but bounded reduction. Strategy PM reduces reward-model scoring time by up to 19x on structured tasks with less than 10% accuracy loss.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代AI工作流（如LLM后训练中的拒绝采样、智能体推理任务）需要大量调用昂贵的模型（如奖励模型、大语言模型），导致计算成本高昂。现有优化方法通常需要修改底层数据库架构或模型本身，部署代价高。
- **整体含义**：本文提出将这些工作流重新表述为**查询中心的**（query-centric）形式，从而借用经典数据库技术——近似查询处理（AQP）和代理模型（PM）过滤——来显著减少昂贵模型调用次数，且无需改动模型或流水线。这揭示了“查询级属性”（如聚合分布、LIMIT语义）本身蕴藏着优化机会，从数据库视角可以系统性地挖掘。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将AI工作流（例如“筛选出前K个最高奖励分数的回答”）转化为标准SQL查询，然后利用查询结构（聚合、排序、过滤）来避免对所有记录执行昂贵模型推理。

### 关键技术细节
- **策略AQP（近似查询处理）**：
  - 将工作流视为在线聚合问题：渐进均匀采样记录，计算聚合估计值及其置信区间（C.I.），当C.I.半宽稳定且小于用户指定误差ϵ时提前停止。
  - 对聚合函数（SUM/AVG/COUNT）使用标准在线聚合公式：估计量 \(\hat{Y} = \frac{1}{n}\sum v_i\)，半宽 \(Z_\alpha s/\sqrt{n}\)。
  - 对**LIMIT查询**（如SELECT * ORDER BY score DESC LIMIT k）：提出两种停止准则——
    - **分布无关理论界**：所有top-k出现在大小为m的样本中的概率 \(P_k(m,N) = \prod_{i=0}^{k-1} (m-i)/(N-i)\)，当 \(P_k(m,N)\ge\alpha\) 时停止。
    - **自适应经验准则**：监测当前第k大观测分数 \(r^{(m)}_{(k)}\)，当 \(|r^{(m)}_{(k)} - r^{(m-1)}_{(k)}|\le \delta\) 时停止，利用实际分数分布。

- **策略PM（代理模型过滤）**：
  - 从少量Oracle标注样本（均匀抽样记录）训练一个轻量级决策树（CPU可运行），提取**表面特征**（响应长度、符号密度、格式标记等），这些特征可作为SQL WHERE子句直接计算。
  - 在推理阶段，对每组候选（如每个prompt的N个回答）运行级联过滤：
    - **方差门（组级跳过）**：若决策树预测分数的组内标准差低于阈值\(\sigma_{\text{th}}\)，则认为所有候选无显著差异，跳过整组，零Oracle调用。
    - **候选过滤（组内修剪）**：保留预测分数高于阈值\(\tau\)的候选子集，仅对该子集调用Oracle进行最终排名；若子集为空则回退到所有N个候选。
  - 两个阈值从训练样本的自引导校准中获得（\(\sigma_{\text{th}}\)设为预测分散度的高分位数，\(\tau\)设为预测分数的中高分位数），并提供两种预设：保守型（仅候选过滤）和激进型（方差门+候选过滤），根据生成器强度自动选择。

- **策略选择规则**：
  - 若查询不含聚合，优先用PM；若含聚合，先尝试AQP；若AQP的C.I.太宽（偏斜数据或小表），则切换到PM或结合使用。两者共享初始5-10%的采样成本。

## 3. 实验设计：数据集、基准、对比方法

- **数据集与场景**：
  - **TPC-DS**：选择8个代表性查询（Q7,13,50,52,53,54,55,56），涵盖SUM/AVG/COUNT聚合函数。将原始谓词分为“可预测谓词”（用标签模拟）和结构过滤，构造二元标签。实验在自然标签分布以及人工调整的30%/50%正例率下进行。
  - **LLM后训练任务**：
    - 数学推理：GSM8K（检查答案准确率）
    - 通用指令遵循：UltraFeedback（使用UltraRM-13B奖励分数作为质量指标）
    - 代码生成：HumanEval+ 和 MBPP（pass@1准确率）
    - 每个prompt生成N=32个候选，奖励模型（Oracle）分别为Skywork-Reward-V2-Qwen3-8B（数学/代码）和UltraRM-13B（通用）。
  - 使用四种生成器：Qwen3-8B, LLaMA-3.1-8B, Mistral-7B-V0.3, Qwen3-32B。

- **基准**：完整调用Oracle（Full-RM）的结果作为ground truth；随机采样策略作为隐式基线（图中用曲线展示不同调用比例下的性能）。

- **对比方法**：本文主要展示AQP和PM自身的性能，没有与FrugalGPT、Speculative Rejection等方法直接对比（作者指出这些方法正交，且本文聚焦于查询级优化而非模型替换或生成侧优化）。另外，在TPC-DS实验中，PM使用了决策树回退机制（当代理不可靠时自动将更多记录发给Oracle）。

## 4. 资源与算力

- **LLM实验**：
  - 前三代生成器（Qwen3-8B, LLaMA-3.1-8B, Mistral-7B-V0.3）在**单张NVIDIA RTX 5090**上运行。
  - Qwen3-32B在**8×A100 80GB GPU**上运行。
  - 论文给出了各任务下Oracle评分时间（如Math任务Full-RM需1079秒），但**未说明模型训练的GPU耗时**，因为本文只调用预训练生成器和奖励模型进行评分（Oracle调用），不涉及生成器本身的训练。
- **TPC-DS实验**：未明确指定GPU/CPU，通常这类表数据实验可在CPU上运行。

## 5. 实验数量与充分性

- **TPC-DS**：8个查询，每种标签分布（自然、30%、50%）下从0.1%到20% Oracle预算，每个配置重复10次，报告均值及25-75百分位区间。此外展示AQP的C.I.随采样轮次的变化，PM的聚合误差与回退率随训练样本比例的变化。
- **LLM实验**：3个任务×4种生成器 = 12个主实验（图7）。每个实验展示两种策略在不同Oracle调用比例下的性能曲线，以及自适应停止点。此外PM在数学/代码上用5%训练样本，通用用10%。
- **消融/内部对比**：PM的两种预设（策略A:仅候选过滤；策略B:方差门+候选过滤）隐含在结果中（图7中PM点位置对应不同预设选择）。论文也讨论了不同任务下PM的表现差异。
- **充分性评价**：实验覆盖了不同聚合函数、不同表大小、不同标签分布、不同LLM领域和模型规模，重复次数合理。但缺少与SOTA优化方法（如CORE、Speculative Rejection）的直接公平对比，不过作者论证了这些方法正交或需要修改查询引擎，本文强调不修改引擎的轻量优势。整体实验设计客观，能支持主要结论。

## 6. 论文的主要结论与发现

- **TPC-DS**：
  - 策略AQP在平衡分布下，自适应停止点出现在10-15%的Oracle调用处，聚合误差<10%（85-90%的调用减少）。
  - 策略PM在5/8个查询上，聚合误差<10%，回退调用率降低，总计Oracle调用减少60-70%；但在小表或预测任务困难时（如Q13、Q54）效果不佳。
- **LLM流水线**：
  - 策略AQP在结构化任务（数学、代码）上达到全RM精度5%以内的损失，而Oracle调用减少20-50%；通用任务上奖励分数损失~15%。
  - 策略PM在数学和代码任务上加速最高19倍（如LLaMA-3.1-8B在Math上从1079s降至56s），精度损失<10%；通用任务上加速较温和（约2x），符合预期（表面特征不具区分性）。
- **总发现**：两种策略互补：AQP提供统计保证且普适；PM在结构领域提供额外加速。经典数据库技术可以大幅降低AI工作流成本而不必修改模型。

## 7. 优点

- **新颖视角**：将AI工作流显式建模为带昂贵谓词的查询，使得数据库优化技术可直接应用，无需修改数据库内核或模型架构。
- **实用性强**：AQP提供统计置信区间控制，用户可指定误差容忍度；PM的决策树模型可转化为SQL谓词，在现有查询引擎内原生执行，部署成本低。
- **自适应机制**：AQP的停止准则（理论界和自适应经验准则）兼顾保证和效率；PM的级联门控根据组内预测差动态跳过整组，避免低信息量的Oracle调用。
- **特征工程巧妙**：利用查询结构提取表面特征（长度、符号密度等），这些特征在LLM结构化任务中具有很强的区分力，且计算极低成本。
- **实验维度丰富**：覆盖传统数据库基准（TPC-DS）和现代LLM后训练任务，评估了多个生成器规模，重复实验增强可信度。

## 8. 不足与局限

- **PM在非结构化任务上效果有限**：通用指令遵循（UltraFeedback）中，表面特征区分力弱，PM加速比只有约2x，且需要更多训练样本（10% vs 5%）。
- **AQP对偏斜数据和小表敏感**：当标签分布极偏斜或表行数很少（如Q13仅6477行）时，C.I.很难收窄，AQP不可靠，需要PM或全Oracle回退。
- **缺乏与其他优化方法的直接比较**：文章未在相同设置下对比FrugalGPT、Speculative Rejection、CORE等方法，虽然论证了这些方法正交或侵入式，但读者难以量化本文方法的相对优势。
- **PM的训练样本比例经验性设定**：5%/10%的选择基于预实验，但未深入分析不同比例下的敏感度，可能在不同任务上需要调整。
- **未考虑多步智能体工作流或RAG**：仅测试了单步LLM后训练和简单聚合查询，未扩展到更复杂的多步骤查询（如JOIN后的语义过滤）。
- **算力信息不完整**：没有报告训练PM决策树或进行实验的总耗时，仅报告了RM评分时间，读者无法评估端到端开销（特征提取+决策树推理 vs Oracle调用）。

（完）
