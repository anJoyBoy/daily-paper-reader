---
title: "KV-PRM: Efficient Process Reward Modeling via KV-Cache Transfer for Multi-Agent Test-Time Scaling"
title_zh: KV-PRM：通过KV缓存传输实现多智能体测试时缩放的高效过程奖励建模
authors: "Peng Kuang, Haibo Jin, Xiaoyu Han, Yanli Wang, Xiaopeng Yuan, Ye Yu, Kaidi Xu, Haohan Wang"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09153v1"
tags: ["query:chemical-llm"]
score: 9.0
evidence: 通过重用LLM的KV缓存实现高效过程奖励建模
tldr: 现有文本PRM在长序列中重新编码整个轨迹，计算成本随序列长度二次增长，限制了测试时扩展的应用。KV-PRM利用LLM生成阶段的KV缓存，仅处理一个验证令牌，将评分成本降至线性。在多个基准上匹配或超越文本PRM，FLOPs降低5000倍，延迟降低37倍，内存减少34倍。为长上下文多智能体场景提供了高效奖励建模方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09153v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09153v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1438, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09153v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1367, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09153v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 685, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 740, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 753, \"height\": 334, \"label\": \"Table\"}]"
motivation: 文本PRM在长序列中重新编码轨迹，计算成本二次增长，限制测试时扩展应用。
method: KV-PRM直接读取LLM生成时的KV缓存，仅处理单个验证令牌，实现线性复杂度评分。
result: 在MATH等基准上达到或超越文本PRM，FLOPs降低5000倍，延迟降37倍，内存降34倍。
conclusion: KV-PRM通过KV缓存复用高效实现过程奖励建模，显著提升长上下文多智能体系统的可扩展性。
---

## 摘要
过程奖励模型（PRMs）已被证明在引导测试时缩放（TTS）方法方面非常有效，这显著提升了基于LLM的多智能体系统的能力。然而，现有的PRMs是基于文本的：它们从头重新编码整个轨迹文本。在长期的多智能体展开中，得分成本随序列长度L呈二次增长，造成了严重的计算瓶颈，严重限制了PRMs在长上下文场景中的应用。为了解决这个问题，我们引入了KV-PRM，一种高效的过程奖励模型，它通过直接读取LLM生成阶段自然产生的KV缓存，消除了繁重的文本重新编码。通过针对已有的KV缓存处理单个“验证令牌”，KV-PRM将得分成本从O(L^2)降低到O(L)。我们正式证明了KV缓存包含比文本严格更多的信息容量，并且对于下游奖励建模更高效。在MATH、GSM8K和AIME基准测试上，经验表明，在各种TTS方法（如Beam Search、MCTS和加权投票）下，KV-PRM匹配或严格优于文本PRMs，得分FLOPs降低高达5000倍，延迟降低37倍，每序列内存占用减少34倍，与基于文本的PRMs相比。

## Abstract
Process Reward Models (PRMs) have been proven to be highly effective in guiding test-time scaling (TTS) methods, which significantly boost the capabilities of LLM-based multi-agent systems. However, existing PRMs are text-based: they re-encode the entire trajectory text from scratch. In long multi-agent rollouts, the scoring cost, growing quadratically with respect to sequence length L, creates a severe computational bottleneck, severely limiting PRMs' application in long-context scenarios. To resolve this, we introduce KV-PRM, a highly efficient process reward model that eliminates the heavy text re-encoding by directly reading the KV cache produced naturally during the LLM's generation phase. By processing a single "verify token" against the pre-existing KV cache, KV-PRM reduces the scoring cost from O(L^2) to O(L). We formally prove that the KV cache contains strictly greater information capacity than text, and is more efficient for downstream reward modeling. Empirically, across the MATH, GSM8K, and AIME benchmarks, KV-PRM matches or strictly outperforms text-PRMs under various TTS methods such as Beam Search, MCTS, and Weighted Voting, with up to a 5,000x reduction in scoring FLOPs, a 37x reduction in latency, and a 34x reduction in per-sequence memory footprint compared to text-based PRMs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有过程奖励模型（Process Reward Models, PRMs）在引导多智能体系统测试时缩放（Test-Time Scaling, TTS）时，因采用纯文本表示而存在严重计算瓶颈：对每条候选轨迹的完整文本进行重新编码，自注意力复杂度为 \(O(L^2)\)（\(L\) 为序列长度），导致长轨迹下评分成本与生成成本相当，严重限制了PRM在长上下文场景中的实际应用。
- **整体含义**：本文试图通过复用LLM生成阶段自然产生的KV缓存（Key-Value Cache）来替代文本重编码，实现线性复杂度评分，从而大幅提升PRM的效率，使测试时搜索能够扩展到更长轨迹和更大搜索空间。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用LLM在自回归生成过程中已计算的KV缓存（包含各层各位置的隐状态）作为评分输入，仅追加一个验证令牌（verify token）进行单步前向传播，通过LoRA适配器输出奖励分数，将评分复杂度从 \(O(d L^2)\) 降至 \(O(d L)\)。
- **关键技术细节**：
  - **架构**：基础LLM（禁用LoRA）正常生成，产生KV缓存；KV-PRM在验证时启用LoRA适配器 \(f_{\theta+\Delta\theta}\)，将验证令牌 \(v\)（如“?”）附加到缓存末尾，通过注意力机制读取KV缓存，最终在“+”和“-”两个判断令牌上取softmax得到得分 \(s_j = P(+)\)。
  - **训练**：使用MCTS自动生成中间步骤的Q值标签 \(y \in [-1,1]\)；对每个样本，先用基础模型无梯度编码轨迹得到KV缓存，再启用LoRA前向验证令牌，计算MSE损失 \(\mathcal{L}_{\text{KV}} = \text{MSE}(P(+), (y+1)/2)\)，仅更新LoRA参数。算法1详细描述了训练流程。
  - **理论支撑**：
    - **定理1**（验证误差分解）：KV缓存的信息容量大于文本（数据不等式与线性表示假设），故基于KV缓存的贝叶斯最优误差不大于文本PRM。
    - **定理2**（深度读出的收益递减）：在低秩奖励结构假设下，额外验证令牌的信息增益随深度指数衰减，单令牌即可捕获大部分可用信息，且信息-代价比例随FLOP衰减，证明了\(k=1\)设计的最优性。
- **公式/算法**：
  - 评分成本对比：文本PRM \(F_{\text{text}} = O(d L^2)\)，KV-PRM \(F_{\text{kv}} = O(d L)\)，加速比 \(O(L)\)。
  - 损失函数：\(\mathcal{L}_{\text{KV}} = \text{MSE}\left(P(+), \frac{y+1}{2}\right)\)。
  - 算法1：训练循环：对每个batch，用\(f_\theta\)（无适配器）编码轨迹得KV缓存，用\(f_{\theta+\Delta\theta}\)前向验证令牌，计算损失，更新LoRA参数。

### 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集**：MATH、GSM8K、AIME 2024、AIME 2025（均数学推理）。
- **评估指标**：精确匹配准确率（exact-match accuracy）。
- **搜索算法**：Step-level Beam Search (SBS)、Monte Carlo Tree Search (MCTS)、Weighted Majority Voting (WV)。
- **模型尺度**：Qwen3系列（0.6B、4B、8B），关闭thinking模式。
- **多智能体拓扑**：Sequential（Reader→Planner→Solver→Verifier）和Hierarchical（Math/Science/Code Agents→Task Summarizer）。
- **对比方法**：
  - No PRM：随机采样 / 多数投票。
  - Policy Log-prob：用生成器自身的对数概率排序。
  - Text-PRM：相同LoRA架构、数据、目标，但需对整个轨迹文本重新编码（\(O(d L^2)\)）。
- **消融实验**：
  - 不同数量验证令牌 \(k\)（1~10）对准确率的影响。
  - 缩小文本PRM模型尺寸（0.6B、4B、8B）与KV-PRM对比效率。
  - KV Steering概念验证（梯度优化缓存）。

### 4. 资源与算力

- **训练硬件**：未明确说明具体GPU型号、数量及训练时长。仅提及推理延迟测试在“NVIDIA GH200 120GB GPU”上进行。
- **训练超参数**（附录表5）：LoRA rank=256, alpha=32, dropout=0.05, max sequence length=8192, effective global batch=2048, bf16精度。训练数据由MCTS（R=64 rollouts, C=4 candidates per step）在MATH训练集上生成。
- **训练成本**：未明确报告总GPU小时数，但指出KV-PRM训练仅需单验证令牌梯度，远小于文本PRM的完整序列梯度计算。

### 5. 实验数量与充分性

- **实验数量**：非常丰富。覆盖4个数学基准、3种搜索算法、3个模型尺度、2种多智能体拓扑、多个成本度量（FLOPs、延迟、内存）。消融实验包括验证令牌数量、PRM尺度对比、层级拓扑验证、KV steering初步实验。
- **充分性与公平性**：
  - 对比公平：KV-PRM与Text-PRM使用相同训练数据、相同LoRA结构、相同目标函数，仅输入表示不同。
  - 统计鲁棒：报告平均准确率（可能多次运行取平均），但未明确说明方差或显著性检验。
  - 覆盖面较全：但对非数学任务（如代码、常识推理）未测试；长上下文仅测试至4096 tokens；多智能体拓扑仅测试两种（Sequential和Hierarchical）。
  - KV Steering仅为概念验证（无搜索，单一梯度步长），实验范围较窄。

### 6. 论文的主要结论与发现

- KV-PRM的评分FLOPs降低约5000倍（对应\(L \approx 5000\)），延迟降低15–37倍，每序列内存减少34.2倍。
- 在所有基准上，KV-PRM匹配或超越了文本PRM的准确率，且常优于后者（例如AIME上提升显著）。
- 理论证明KV缓存的信息密度高于文本，且单令牌读出的信息损失可忽略，为高效设计提供了严格依据。
- KV Steering展示了基于梯度优化潜空间消息的可行性（无需搜索即获得准确率提升），是文本PRM无法实现的新能力。

### 7. 优点

- **创新性高**：首次提出将KV缓存直接用于奖励建模，替代冗余文本重编码，从根本上解决计算瓶颈。
- **理论扎实**：提供了严格的证明（线性和低秩假设下）支持KV缓存的信息优势以及单令牌读出的近似最优性。
- **实证充分**：广泛的实验覆盖多种搜索算法、模型规模、拓扑结构，效率与准确率双赢。
- **实用性强**：KV-PRM可作为现有TTS流水线的即插即用模块，无需改动搜索算法；训练仅需轻量LoRA。
- **新方向**：KV-Steering开辟了潜空间优化测试时计算的新范式。

### 8. 不足与局限

- **架构耦合**：要求生成器和验证器共享同一基础模型（因KV缓存由同一模型产生），无法跨架构复用。
- **理论假设**：部分证明依赖“线性表示假设”（Assumption 1）和“低秩奖励结构”（Assumption 2），这两个假设在复杂语言任务中可能不完全成立，定理的实际紧度需进一步检验。
- **KV Steering尚属初步**：仅展示简单梯度上升，未深入探讨收敛性、扰动稳定性或与搜索结合的效果，实用性有限。
- **任务覆盖不足**：仅测试数学推理，未涵盖代码、常识问答、多模态等场景；序列长度仅至4096，更长轨迹的性能需验证。
- **训练数据成本**：标签依赖MCTS生成，MCTS本身的计算成本未纳入对比（虽不影响推理效率）。
- **统计显著性**：未报告多次运行的误差条或置信区间，难以判断差异是否统计显著。

（完）
