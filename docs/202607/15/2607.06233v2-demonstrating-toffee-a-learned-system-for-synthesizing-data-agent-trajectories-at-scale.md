---
title: "Demonstrating TOFFEE: A Learned System for Synthesizing Data Agent Trajectories at Scale"
title_zh: 演示TOFFEE：一种大规模合成数据代理轨迹的学习系统
authors: "Ziting Wang, Yin Li, Zuhao Yang, Xiuchang Li, Jiale Bai, Gao Cong"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06233v2"
tags: ["query:llm-dm"]
score: 8.0
evidence: 大语言模型驱动的数据代理轨迹合成用于数据挖掘任务
tldr: 现有LLM驱动的数据代理在异构企业环境中泛化能力不足，亟需高质量轨迹数据。TOFFEE系统利用蒙特卡洛树搜索、自适应模型选择和跨任务前缀重来自动合成数据代理轨迹。实验表明，该方法可有效生成复杂分析任务的可扩展轨迹。该工作为数据代理的微调和上下文学习提供了高质量训练数据，显著提升其跨环境泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06233v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 791, \"height\": 784, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 795, \"height\": 793, \"label\": \"Figure\"}]"
motivation: 现有数据代理难以泛化到未见的异构数据环境和工作流，需要合成高质量轨迹以支撑模型微调和上下文学习。
method: 提出TOFFEE系统，采用蒙特卡洛树搜索（MCTS）结合自适应模型选择与跨任务前缀重用来高效合成轨迹。
result: TOFFEE在异构环境中可生成高质量、可扩展的数据代理轨迹，覆盖复杂分析任务。
conclusion: TOFFEE有效生成轨迹数据，支撑数据代理微调与增强推理，提升跨环境泛化性能。
---

## 摘要
基于LLM的数据代理在数据驱动决策中扮演着越来越重要的角色。然而，现有的数据代理难以泛化到未见过的数据环境和分析工作流，尤其是在异构的企业环境中。这产生了对合成高质量数据代理轨迹的日益增长的需求，这些轨迹能够捕获给定数据环境中的复杂分析工作流。此类轨迹支持两个关键的 downstream 用途：它们可以作为监督微调（SFT）数据，使数据代理模型适应目标领域；也可以作为上下文学习（ICL）演示，引导通用LLM在陌生数据环境中工作。因此，我们引入了TOFFEE，一个通过蒙特卡洛树搜索（MCTS）结合自适应模型选择和跨任务前缀复用来从给定数据环境合成高质量数据代理轨迹的系统。我们展示了TOFFEE能够有效地为跨异构环境的复杂分析任务生成可扩展的轨迹数据。在本演示中，我们介绍了TOFFEE的系统框架，包括其任务池构建、轨迹探索器和学习成本模型。我们还介绍了TOFFEE的Web界面及其工作流，并展示了两个端到端场景：用于数据代理微调的轨迹合成，以及增强演示的数据代理推理。

## Abstract
LLM-powered data agents are playing an increasingly important role in data-driven decision making. However, existing data agents struggle to generalize to unseen data environments and analytical workflows, especially in heterogeneous enterprise settings. This creates a growing need for synthesizing high-quality data agent trajectories that capture complex analytical workflows for given data environments. Such trajectories support two key downstream uses: they can serve as supervised finetuning (SFT) data that adapts data agent models to the target domain, and as in-context learning (ICL) demonstrations to guide general-purpose LLMs in unfamiliar data environments. Thus, we introduce TOFFEE, a system for synthesizing high-quality data agent trajectories from given data environments via Monte Carlo Tree Search (MCTS) with adaptive model selection and cross-task prefix reuse. We show that TOFFEE can effectively generate scalable trajectory data for complex analytical tasks across heterogeneous environments. In this demonstration, we present the system framework of TOFFEE, including its task pool construction, trajectory explorer, and learned cost model. We also introduce the web interface of TOFFEE and its workflow, and demonstrate two end-to-end scenarios: trajectory synthesis for data agent finetuning, and demonstration-augmented data agent reasoning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于LLM的数据代理在异构企业环境中难以泛化到未见过的数据环境与分析工作流，导致SQL执行错误、结论模糊、缺乏具体统计证据。主要原因包括：单次生成失败即丢弃、最佳N采样存在冗余计算、任务池构建依赖人工且质量低、推理配置选择缺乏自适应。
- **整体含义**：高质量数据代理轨迹（多步推理、工具调用和执行结果的序列）对于通过监督微调（SFT）适应目标领域或作为上下文学习（ICL）演示至关重要。TOFFEE旨在自动化、规模化地合成此类轨迹，提升数据代理的跨环境泛化能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过蒙特卡洛树搜索（MCTS）结合自适应模型选择与跨任务前缀复用，在预算约束下高效探索轨迹空间，同时利用学习成本模型（LCM）动态选择每步推理配置。
- **关键技术细节**：
  - **任务池构建**：先对数据环境进行概要分析，通过预定义SQL模板发现数据依赖关系，再用LLM将每个依赖转化为有意义的问题，并执行验证确保任务可解且答案具有确定性。
  - **轨迹探索器**：以MCTS为基础，每个节点代表分析状态，边表示由特定LLM配置生成的工具调用（SQL、Python等）。搜索从跨任务共享前缀开始（如schema发现），遇到错误时不丢弃而是分支进入修复路径，保留错误诊断-修复序列作为训练数据。轨迹被存储当确定性验证器确认最终答案匹配任务答案键。
  - **学习成本模型（LCM）**：从当前状态（轨迹进度、schema覆盖率、最近错误历史）提取特征，为每个可行的配置（操作类型、模型、上下文长度、推理努力）预测奖励𝜇和探索奖励𝜎，通过ridge回归在线更新参数。根据顶端配置的差异度设置分支宽度K，预算缩减时减小K。
- **流程**：输入数据环境 → 任务池构建 → 轨迹探索（MCTS+LCM）→ 导出为SFT或ICL样本。

### 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：使用了**KramaBench**和**DSBench**两个数据代理基准，以及用于轨迹合成的环境包括**Spider**、**LiveSQLBench**、**SpreadsheetBench**等源语料库，以及用户连接的真实异构环境（如学校数据库）。
- **benchmark**：KramaBench和DSBench用于评估下游任务性能（分数为0~1）。
- **对比方法**：
  - 轨迹质量对比：**Single-Pass**（单次生成）、**Best-of-N**（N=5）、**TOFFEE w/o LCM**（无学习成本模型的MCTS）、**TOFFEE**（完整系统）。
  - 下游效果对比：**OpenAI o3**（前沿推理模型）、**Qwen3.5-27B零样本**、**ICL（TOFFEE轨迹演示）**、**TOFFEE-27B（SFT）**。

### 4. 资源与算力

- 论文明确提到**在Ubuntu服务器上进行实验**，合成工作通过API调用LLM（通过OpenRouter），**未具体说明GPU型号、数量或训练时长**。仅说明TOFFEE-9B和TOFFEE-27B是从Qwen3.5-9B和Qwen3.5-27B微调得到，但未提供微调所需的算力细节。

### 5. 实验数量与充分性

- **实验数量**：文中展示了两个主要实验图：
  - 图3a：轨迹质量 vs. 预算曲线（单次实验，对比四种方法在不同预算下的表现）。
  - 图3b：下游结果柱状图（KramaBench和DSBench两个数据集，对比零样本、ICL、SFT、o3）。
- **充分性评价**：实验覆盖了轨迹合成效率、下游泛化能力，但缺少消融实验（如LCM不同组件的贡献、不同模型池的影响）、跨更多数据环境的验证、统计显著性检验。作为演示论文（PVLDB Demo），实验量相对有限，但足以展示系统核心优势。

### 6. 论文的主要结论与发现

- TOFFEE在相同预算下合成的轨迹质量显著高于Single-Pass和Best-of-N，且通过自适应模型选择（LCM）比不带LCM的MCTS更高效，收敛更快、成本更低。
- 使用TOFFEE轨迹进行SFT得到的TOFFEE-27B在KramaBench和DSBench上超越OpenAI o3；仅使用ICL演示也能显著提升零样本基线。
- 验证了跨任务前缀复用和错误修复路径的有效性：能够生成包含真实错误诊断与恢复的有用训练数据。

### 7. 优点：方法或实验设计上的亮点

- **任务池构建的自动化**：通过数据依赖发现和验证，确保任务真实、可解、无泄漏，避免LLM直接生成带来的质量问题。
- **自适应推理配置选择（LCM）**：区分“routine步骤”和“复杂分析步骤”，动态分配预算，提高资源利用率。
- **MCTS探索与错误处理**：保留错误修复路径作为训练数据，提升下游代理的鲁棒性；跨任务前缀复用减少冗余。
- **支持SFT和ICL两种下游用途**：提供灵活导出选项，适应不同用户需求。
- **交互式Web界面**：实时可视化搜索树、质量曲线、成本指标，增强可操作性和理解性。

### 8. 不足与局限

- **实验覆盖范围较小**：仅使用两个benchmark、一个数据环境示例，缺乏在更多异构环境（如多模态表格、非结构化文本）上的验证。
- **算力与成本细节缺失**：未报告微调TOFFEE-9B/27B的具体资源、耗时，也未给出轨迹合成的总成本（如API调用次数、预算规模），不利于复现和成本评估。
- **模型池与LCM配置的泛化性**：LCM基于在线学习的bandit方法，可能对新的配置空间（如不同LLM系列）需要重新训练或调参。
- **缺乏统计显著性测试**：图表未显示误差条或置信区间，仅展示单一运行结果。
- **潜在的数据污染风险**：虽然声称通过执行验证确保任务隔离，但任务生成依赖LLM（可能使用公开数据），仍存在一定风险。
- **应用限制**：轨迹合成依赖沙盒执行（sandboxed code），对数据环境的安全性和可访问性有要求；LLM API调用成本可能随任务复杂度增加。

（完）
